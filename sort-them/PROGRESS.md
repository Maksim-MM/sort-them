# PROGRESS: состояние прототипа

## Где мы сейчас (09.09)
Этапы 0–4 закрыты, из этапа 6 сделано тач-управление с эмуляцией в редакторе (раздел «Мобильное управление и UI»), из этапа 5 — коллекционки, звуки-заглушки, радио. Прототип играется. Идёт доводка ядра по фидбеку: пользователь тестирует в Play Mode, сравнивает с референсом Sort Them Ducks, правки принимаются по одной (см. раздел «Правки по утреннему фидбеку» и блок «Доводка ядра» в `WORK_PLAN.md`). Утверждённые решения переносятся в `GDD.md` сразу.

Дальше по плану: этап 5 (контент и зал: атлас, реальные модели, круглая полка, коллекционки, звук), этап 6 (тач, обучение, меню), этап 7 (Яндекс SDK, публикация). До них — открытые вопросы из чек-листа и баланс дальностей.

Рабочий режим: перед правками C# я выхожу из Play Mode сам и предупреждаю; правки шейдеров и ассетов домен не перезагружают.


Файл состояния для продолжения работы после сжатия контекста или обрыва сессии.
Источник задач: `WORK_PLAN.md` (этапы 0–4), механики: `GDD.md`, производительность: `OPTIMIZATION.md`, требования к ассетам: `ASSET_REQUIREMENTS.md`.
Правила: без git-записи, без фич сверх GDD, всё в `Assets/_Game/`.

## Итог ночи (06.09.2026, ~01:20–04:40)
Этапы 0–4 плана закрыты в объёме прототипа: зал 36×30 м, 20 стеллажей × 5 полок (100 полок по 10 слотов), 100 сгенерированных моделей-заготовок в 10 категориях, 1000 машинок с запечённым разбросом, все механики ядра, 9 апгрейдов, 3 активные способности, сейв в PlayerPrefs, локализация RU/EN, управление клавиатура+мышь и геймпад (геймпад не тестировался, нет устройства). WebGL-сборка собирается. Скриншоты: `Docs/screens/`.

## Как проверить утром
1. Открыть `Assets/_Game/Scenes/Main.unity`, Play. Тестовый сейв очищен, игра начинается с нуля.
2. Управление: WASD, мышь, ЛКМ взять, ПКМ поставить/бросить, колесо — выбор в инвентаре, Esc — пауза (Сохранить / Вернуть застрявшие / Сбросить сохранение). Терминал апгрейдов: тёмный шкаф с надписью UPGRADES в углу у точки старта, ЛКМ по нему. Спринт/присед/способности 1-2-3 работают только после покупки.
3. Меню `SortThem/`: 1 Localization Setup, 2 Input Actions, 3 Generate Cars, 3b Create Upgrades, 4 Build Level Scene, 5 Bake Scatter Layout, Build WebGL, `Setup All (1-4)`. Пересборка сцены (4) затирает `Main.unity` и требует перезапечь разброс (5).
4. Меню `SortThem/Dev/Tick Play Mode In Background` — dev-костыль для ночной работы, при живой работе лучше выключить.
5. Тесты: Window → General → Test Runner → EditMode, 18 тестов (14 логики + 4 инварианта контента по сцене и раскладке).
6. Баланс/параметры: `Assets/_Game/Data/Config/GameConfig.asset` (скорости, дальность взаимодействия 2.2 м, дистанция броска 1.5 м, радиус сферы физики 3 м, кулдауны), `EconomyConfig.asset`, `Data/Upgrades/*.asset` (цены и значения уровней).

## На обсуждение
- Пункты раздела «Решения без пользователя» ниже.
- Модели-заготовки: 100 штук различаются семейством (10), длиной (3 шага) и аксессуаром (4 варианта) плюс цвет. Проверить глазами, достаточно ли отличий.
- Не сделано из этапов 0–4: замер FPS в браузере на 300/1000 (нужен браузер), тест геймпада, тест WebGL-сборки в браузере.

## Правки по утреннему фидбеку (06.09)
- [x] Полка 2 ряда × 5 слотов (`ShelfData.Rows/Columns`, `Capacity` вычисляется). Стеллаж 2.45 × 1.1 м, зал уменьшен до 22 × 16 м, 20 стеллажей на местах, разброс перезапечён (скорости 5–9 м/с).
- [x] Установка и зелёный силуэт идут в первый свободный слот по порядку: задний ряд справа налево, затем передний. Авторасстановка при броске использует тот же порядок. Снятая из середины машинка освобождает слот, он заполняется первым.
- [x] GDD обновлён (ShelfData, правило установки). Тесты 14/14. Скриншот `Docs/screens/shelf_2x5_fill.png`.
- [x] Ценник уменьшен (0.5 × 0.08 м), висит на торце полки под передней кромкой, две строки: название и цена. Счётчик X/Capacity убран.
- [x] Снятие с полки в обратном порядке: прицел на полку или на любую машинку на ней подсвечивает контуром последнюю поставленную, ЛКМ забирает её (`ShelfController.LastPlaced`). Машинки на полу берутся как раньше, прямым прицелом.
- [x] GDD обновлён (ценник, правило ЛКМ). Скриншоты `Docs/screens/shelf_take_outline.png`, `price_tag_*.png`.
- [x] HUD инвентаря как в референсе: справа внизу вертикальный список по порядку, активная строка белая и на 30% крупнее, остальные серые, под списком счётчик X/Y жирным. Нижняя полоска и отдельная подпись «в руках» убраны. Скриншот `Docs/screens/hud_inventory.png`.
- [x] Dev-тикер BackgroundPlayTicker выключен по умолчанию (вызывал «паузу» Play Mode при потере фокуса окна).
- [x] Активная машинка показывается в руках (`HeldItemView` на камере): меш активной модели справа внизу, материал `CarsHeld` с шейдером `VertexColorLitOverlay` (обычный ZTest, но глубина модели сжата в вершинном шейдере в узкий диапазон у ближней плоскости: стены её не перекрывают, а самоперекрытие частей модели сохраняется; ZTest Always ломал порядок колёс и корпуса). Смена: старая исчезает за 0.08 с, новая въезжает из правого нижнего угла за 0.28 с. Положение/поворот/масштаб/длительности — поля компонента (утверждённый ракурс: позиция (0.36, −0.26, 0.8), поворот (0, 145, 0), масштаб 0.85: три четверти спереди, чуть сверху). Текст списка уменьшен до 18. Скриншот `Docs/screens/held_item.png`.

- [x] Взятие машинки: view-model стартует из мировой позы машинки (позиция и поворот, как лежала на полу или стояла на полке) и за 0.28 с перелетает в руку с доворотом и уменьшением до масштаба руки. Взятая машинка становится активной (раньше активная не менялась).
- [x] Конфиг дальностей в `GameConfig`: `BaseInteractRange` 2.2 м (луч от камеры на высоте 1.65 м, по горизонтали это ~1 м как в референсе), `BaseThrowDistance` 1.5 м (скорость броска считается из дистанции, `ThrowArc` 0.4 — подъём траектории). Апгрейды «Большая дальность» и «Более сильный бросок» умножают эти значения (`ValuePerLevel` в ассетах апгрейдов). Черновые цифры, балансировать тестами.

- [x] Бросок из руки: физическая машинка спавнится в мировой позе модели в руке (позиция и поворот) и летит по дуге; модель в руке исчезает мгновенно, следующая из инвентаря въезжает из угла. При установке на полку та же логика. Если между камерой и рукой стена или предмет, точка вылета отодвигается к камере (луч по `InteractMask`), чтобы машинка не родилась внутри геометрии.

## Сверка с исследованием референса (06.09, день)
Пользователь принёс `Research_SortThemDucks.md`, сравнили с GDD по механикам. Решения:
- Награда $1 за машинку остаётся. Цены апгрейдов со скриншота — первый уровень; цены каждого уровня уже задаются в `UpgradeData.CostPerLevel`, отдельный конфиг не нужен. Баланс экономики — в конце, при боевых ассетах; 1000 машинок — тестовый объём.
- Добавлен тест `ContentInvariantTests` (4 проверки, все зелёные): экземпляров каждой модели = вместимость полки, полок в категории = моделей, машинок = слотов. Полки считаются по сцене Main, а не по RackData-ассетам: один ассет делят два стеллажа сцены.
- В GDD внесены: требования к звуку (раздел 1), касса (раздел 6), башня по цвету отложена (бэклог).
- Не трогаем: бомбы, достижения, читаемость моделей, покачивание камеры (в текущей версии референса его нет).
- Открыто: раскладка способностей 1/2/3 по скриншоту референса (1 подсветка совпадающих в куче + левитация на уровень глаз, 2 автосбор совпадающих, 3 подсветка полок) не совпадает с нашей (1 полка, 2 дубликаты, 3 автосбор) — ждёт решения пользователя. Таблица наград слот-машины: в исследовании нет, нужен скриншот из игры.

## Правки по фидбеку, вторая волна (06.09, день)
- Способности переставлены как в референсе: 1 «Поиск совпадений» (фиолетовый контур `OutlinePurple` + левитация на `GameConfig.LevitateHeight` 1,4 м со скоростью `LevitateSpeed` 3 м/с, машинки крутятся, по истечении падают), 2 автосбор, 3 «Подсветка стеллажа» (рамка `HighlightFrame` на стеллаже, материал сквозь стены). Флаг `CarInstance.Levitating` защищает от сферы активации. Скриншот `Docs/screens/ability_find_levitate.png`.
- Стеллажи объединены: 10 стеллажей по одному на категорию, две секции по 5 полок, общая вывеска. Зал прежний 22×16 м, стеллажей по стенам 3/2/3/2.
- Полки опущены под референс: шаг 0,42 м, высоты 0,25/0,67/1,09/1,51/1,93, стеллаж 2,2 м. Генератор ужимает модели выше 0,32 м (`CarModelGenerator.MaxHeight`), раньше монстр-траки были 0,52 м. Скриншоты `rack_merged_2m.png`, `rack_merged_filled.png`.
- Разброс перезапечён, сейв сброшен (раскладка и ID полок изменились).
- Тесты 18/18 на новой сцене.
- Автосбор переделан: свои `AutoCollectCooldown` 60 с и `AutoCollectDuration` 10 с, притягивание постепенное (`AutoCollectInterval` 0,35 с между машинками, полёт `AutoCollectFlightTime` 0,4 с по дуге в руку), ближайшие первыми, радиус `AutoCollectRadius` 15 м (было 6, из-за этого инвентарь не добирался). Лимит притягивания фиксируется при нажатии (`_collectBudget` = свободные слоты), выкладка на полку во время действия новых не притягивает; действие завершается, когда лимит выбран. HUD показывает остаток действия зелёным.
- Чит H: +$1000.
- Автосбор: лимит фиксируется при нажатии (см. GDD).
- Система сообщений: `Messages.Show(text)` → `UiRoot` показывает тост внизу по центру (плашка 560×46, появление 0,15 с, показ 2 с, затухание 0,4 с, новое заменяет старое). Первые сообщения в способностях: на откате «Способность ещё не готова», с пустыми руками «Возьми что-нибудь в руки» (ключи `msg.*`). Скриншот `Docs/screens/toast_message.png`. Плашка частично перекрывает машинку в руке, при необходимости поднять.
- Авторасстановка: общий триггер стеллажа `RackZone` (бокс на весь стеллаж с запасом 0,15 м) вместо зон полок; магнит вызывается из `RackController.OnLooseCarInside` только для летящей машинки (не кинематик, скорость > 0,5 м/с), поэтому лежащие на крышке при старте не срабатывают. Зоны полок оставлены для отскока чужой модели. Сцена пересобрана.
- Левитация стартует с высоты, где лежала машинка: цель = своя высота + `LevitateHeight` (1,4 м), а не общая высота глаз; из кучи взлетают выше. Луч-ограничитель вверх игнорирует слой `LooseItems`: раньше машинка под другой машинкой не взлетала.
- Прокрутка инвентаря: два порога. Дельта ≥ `ScrollNotchThreshold` (0,9) за кадр = щелчок колеса, переключение сразу; мелкие дельты тачпада копятся до `ScrollThreshold` (0,6 → 2,0), пауза 0,15 с. Отладка: в редакторе при зажатой L дельты скролла пишутся в консоль, чтобы подобрать пороги под конкретный тачпад.
- Поиск совпадений следует за предметом в руке: `UpdateLevitation` сравнивает модель активного предмета с `_findModel` и перезапускает `StartLevitation` при смене; при пустых руках всё опускается. Активация теперь тратит откат даже без совпадений на полу (раньше без совпадений откат не тратился).
- Цены апгрейдов сняты с 18 скриншотов референса (`~/Downloads/Prices`, порядок по времени создания) и записаны в GDD раздел 5: инвентарь +10 за уровень от 30 (15 уровней, +1 слот, до 20), дальность 20/30/40/50, бросок 20/50/70, остальное бинарное (50/30/100/100/150/100). Итого за всё $2310. `UpgradeSetup.CreateAll(resetBalance)`: сборка сцены больше не перезаписывает цены в ассетах, сброс к референсу — меню `SortThem/3c`. Множители дальности и броска — наши допущения (референс их не показывает).
- Эксперимент по броску: `BaseThrowDistance` 1,5 → 0,75 (скорость 5,5 → 3,9 м/с). Число в конфиге не равно дальности приземления, калибровка формулы под реальную дистанцию предложена, решение за пользователем.
- Поиск совпадений: свои `FindDuration` 30 с и `FindCooldown` 60 с Подсветка стеллажа: `RackHighlightDuration` 30 с, `RackHighlightCooldown` 60 с (переименованы из общих `AbilityDuration`/`AbilityCooldown`).
- Поиск совпадений доработан: покачивание по вертикали (`LevitateBobAmplitude` 0,06 м, `LevitateBobSpeed` 2,5), фиолетовый контур сквозь стены: новый шейдер `OutlineXRay` в два прохода — обычный контур-оболочка, когда машинка видна, и полупрозрачный силуэт `ZTest Greater` только там, где её перекрывает геометрия (первый вариант с ZTest Always на оболочке закрашивал корпус сплошным цветом). Нюанс: на Metal (обратная ось Z) правильно работает `ZTest Less`, на обычной оси нужен `Greater`; Unity тест сам не переворачивает, поэтому `PlayerAbilities.Start` выставляет `_XRayZTest` по `SystemInfo.usesReversedZBuffer`. **Проверить в WebGL-сборке** (там ось обычная, ожидается Greater). Шейдер `Outline` получил параметры `_ZTest`/`_ZWrite`, не используются, при наведении в зоне взятия фиолетовый контур скрывается и остаётся белый.
- Проверка постамента с заполненными верхними полками (скриншоты `shelf_yaw35_top_2m.png`, `shelf_yaw35_top_1m.png`): верхняя полка на 2,23 м при глазах 1,70 читается плохо. С 2 м виден только передний ряд, низкие машинки (0,15 м) скрыты кромкой полки: из 6 бирюзовых виден 1. С 1 м видны только крыши. Вывод: постамент 0,55 и шаг 0,42 вместе слишком высокие; варианты — постамент ниже (0,40), шаг 0,38, или глаза игрока выше. Решение пользователя (06.09, вечер): оставить как есть (постамент 0,55, шаг 0,42, поворот 35°), вернуться при боевых ассетах — возможно, там не будет таких длинных моделей, как автобусы.
- Раскладка на полке: `ShelfData.SlotYaw` (поворот в слоте, поставлен 35° — три четверти), `LevelBuilder` больше не затирает `ShelfData` при сборке. Генератор получил лимит длины 0,42 м (`MaxLength`): самый длинный автобус был 0,74 м и не влезал в ряд 0,5. `RowPitch` 0,5 → 0,48, глубина стеллажа 1,1 → 1,06. Спецификация коробки игрушки записана в GDD раздел 2.
- Постамент под стеллажами (проба по просьбе пользователя): нижняя полка 0,25 → 0,55 м (`LevelBuilder.BottomShelfHeight`), под ней сплошной блок `Plinth`. Полки 0,55/0,97/1,39/1,81/2,23, крышка 2,61. Цена: верхняя полка теперь на 0,53 м выше глаз, задний ряд на ней с пола не виден. Обсуждается параметризация стеллажа (см. WORK_PLAN).
- Установка на полку анимирована: машинка летит из позы руки в слот (`ShelfController.TryPlaceAnimated`, `PlaceFlightDuration` 0,3 с), по прибытии пружинит масштабом (`PopScale` 1,15, `PopDuration` 0,25 с). При закрытии полки все машинки пружинят вместе и играет `GameConfig.ShelfCompleteClip` через `Sfx.Play` (клип пока не назначен, поле пустое). Магнит авторасстановки использует тот же полёт.
- Авторасстановка при броске переписана по референсу: магнит только в стеллаж своей категории; полка выбирается стеллажом (`RackController.FindAutoPlaceShelf`): начатая полка модели с местом → пустая полка → ничего. Раньше зона любой полки притягивала любую машинку в первый свободный слот, в том числе в чужой стеллаж и не на ту полку.
- Баг view-model: при броске, если следующая машинка той же модели, анимации доставания не было. `HeldItemView` сравнивал активный предмет по `CarItemData` и выходил до проверки удаления. Теперь сравнение по экземпляру `CarInstance`, проверка удаления идёт первой; смена колесом между одинаковыми моделями тоже анимируется.
- Терминал апгрейдов стоял внутри стеллажа после объединения стеллажей (позиция считалась под старую сетку из 6 слотов). Перенесён в проём между южными стеллажами (x = RoomX/6). При наведении терминал обводится тем же белым контуром, что и машинки. Экран и надпись терминала были обращены к стене, развёрнуты к залу.
- Ходьба по куче: радиус капсулы 0,35 → 0,45 (`GameConfig.PlayerRadius`, применяется при старте), высота камеры сглаживается `SmoothDamp` в `LateUpdate` (`CameraHeightSmoothTime` 0,12 с, в воздухе ×0,25, ограничение отставания 0,4 м). Проверять ощущения на куче и при прыжке.
- Верхняя полка была ниже остальных: просвет 0,27 м против 0,38. Крышка стеллажа теперь считается от последней полки: `RackH = 1,93 + 0,42 − 0,04 = 2,31`. Сцена пересобрана, разброс перезапечён, сейв пользователя не трогался (позиции машинок берутся из сейва).
- Прокрутка инвентаря переписана: действие `ScrollItems` (ось `<Mouse>/scroll/y`), накопление дельты с порогом 0,6, пауза 0,12 с, сброс накопления через 0,25 с без ввода. Причина бага: привязки-кнопки `scroll/up`/`scroll/down` переключали предмет на каждую дельту тачпада. Пороги подбирать на тачпаде пользователя и в WebGL.
- Бэкапы и миграция сейвов отложены до этапа SDK Яндекс Игр (GDD раздел 8, WORK_PLAN этап 7).
- **Баг сейва найден и исправлен (06.09, вечер):** при пустом инвентаре `Serialize` писал байт `ActiveIndex`, а `Deserialize` его не читал, поток смещался, и загрузка отбрасывала сейв с предупреждением «save car count mismatch» → «new game». Любой сейв, сделанный с пустым инвентарём, не загружался. Теперь байт пишется и читается всегда, версия формата не менялась (сейвы с непустым инвентарём совместимы). Сейв пользователя с $850 и расставленными полками был уже нечитаем к моменту теста и затем перезаписан моей очисткой; восстановить нельзя, прогресс потерян. Урок: перед тестами в Play Mode делать копию ключа `sortthem.save` (см. открытый вопрос о резервной копии).

## Правки по фидбеку, третья волна (06.09, вечер)
- **Автосбор больше не мельтешит анимацией руки.** Было: каждая притянутая машинка через `Inventory.Add` становилась активной, и `HeldItemView` проигрывал полную смену модели (уход 0,08 с + прилёт 0,28 с = 0,36 с) при интервале притягивания 0,35 с. Анимация обрывалась следующей на последнем кадре, отсюда рваность и разворачивающиеся в воздухе грузовики. Стало: машинка в руке остаётся прежней, притянутые заходят за неё и уходят в инвентарь молча. Как в референсе.
- `Inventory.Add(car, bool makeActive = true)`: при `false` не меняет `ActiveIndex` и не трогает `LastAdded`/`LastAddedPosition`/`LastAddedRotation`/`AddCount`. Эти поля нужны только `HeldItemView` для анимации прилёта из мировой точки, поэтому нетронутый счётчик заставляет `OnInventoryChanged` выйти сразу, без переигрывания. Ручной подбор (ЛКМ) и откат постановки в `PlayerInteraction` вызывают метод без флага и работают как раньше. Если инвентарь пуст, слот берётся в любом случае.
- `PlayerAbilities.StackTarget(out Vector3, out Quaternion)` заменил `HandTarget()`: цель полёта — точка за моделью в руке, смещение `StackDepth` 0,12 м вдоль луча от камеры через модель. Смещение именно вдоль луча, а не по `camera.forward`: модель в руке висит справа-снизу (`HeldItemView.RestPosition`), и при смещении по взгляду цель уезжала на 74 px к центру экрана; вдоль луча расхождение 0,0 px (замерено в Play Mode на разрешении 2304×1580). Ссылка на `HeldItemView` берётся в `Start` через `FindFirstObjectByType`, как в `PlayerInteraction`. Если поза модели недоступна (идёт анимация смены), используется прежняя точка у руки.
- Летящая машинка больше не крутится (`transform.Rotate(0, 360°/с, 0)`), а доворачивается `Quaternion.Slerp` из своего поворота в поворот модели в руке. Стартовые повороты хранятся в новом списке `_pullRot` рядом с `_pulls`/`_pullFrom`/`_pullStart`, чистится в `RemovePull`.
- Проверено: компиляция без ошибок, EditMode 19/19 (0,5 с). В Play Mode подтверждено кодом: при автосборе активный предмет, `ActiveIndex` и `AddCount` не меняются, `Items` растёт, притянутая машинка скрыта и в состоянии `Held`; ручной подбор по-прежнему делает машинку активной и наращивает `AddCount`.
- Не проверено вживую: полный цикл способности с купленным апгрейдом. Без фокуса окна редактор не крутит игровой цикл (тикер `SortThem/Dev/Tick Play Mode In Background` выключен), машинки не успевали заспавниться после рекомпиляции. Геометрия цели замерена на живых данных, но сам полёт глазами не смотрел — посмотреть при следующем ручном запуске и при необходимости подкрутить `StackDepth` в инспекторе.
- Замечание на будущее: `AutoCollectInterval` 0,35 с меньше, чем `AutoCollectFlightTime` 0,4 с, поэтому в воздухе одновременно находится несколько машинок. Само по себе это уже не мельтешит, но если летящих покажется слишком много, уменьшать надо время полёта, а не интервал.
- **Панель способностей иконками вместо текста** (по референсу). Три плашки 72×72 слева внизу, белые линейные иконки (`Assets/_Game/Art/UI/ab_*.png`, нарисованы кодом: CoreGraphics через Swift, исходники команд в скретчпаде сессии), клавиша над плашкой (1/2/3 или стрелки крестовины по последнему устройству). Действие: фиолетовая радиальная заливка `Image.FillMethod.Radial360` от верха против часовой, откат: светлая полупрозрачная поверх приглушённой иконки (тёмная на тёмной плашке не читалась). `PlayerAbilities.CooldownTotal/ActiveTotal` для долей. `UiFactory.Image` добавлен. Ключи `ui.ab1..3` из `LocalizationSetup` убраны (строки в таблице остались как мусор до следующего пересбора таблицы). Спрайты подключаются в `LevelBuilder`, импорт настроек `SortThem/3d. Import UI Sprites`. Решение: способность 1 тратит откат всегда, даже без совпадений в куче.
- **Иконки в терминале апгрейдов** (07.09). Шесть новых иконок `up_inventory/up_range/up_sprint/up_crouch/up_throw/up_autoplace` в том же стиле, три способности переиспользуют иконки панели. `UiSpriteSetup.AssignUpgradeIcons` пишет `UpgradeData.Icon` по `UpgradeKind` (вызывается из `SortThem/3d`). В строке терминала иконка 52×52 слева, название и описание сдвинуты на 72 px.
- Спринт и присед перерисованы с фигурой человека по замечанию пользователя (бегун с линиями скорости; присевший на согнутых ногах, руки согнуты как у бегуна, без полки и линий движения по замечанию пользователя). Иконки на машинках для этих двух не подходили: апгрейд про игрока, не про предмет.
- **Геймпад в меню** (07.09). Новая карта действий `UI` (`Navigate` dpad/стик/стрелки, `Submit` A/Enter, `Cancel` B) в `InputSetup`, асет пересоздан пунктом 2. `UiRoot.UpdateMenuFocus`: фокус по доступным кнопкам с автоповтором, пульсация масштаба в фокусе только при активном геймпаде (`GamepadActive` по `lastUpdateTime` устройств), Submit жмёт `onClick`, Cancel закрывает. Кнопкам `UiFactory.Button` выставлена `Navigation.Mode.None`, у `InputSystemUIInputModule` обнулены move/submit/cancel: иначе EventSystem жал бы выбранную мышью кнопку параллельно. Проверено виртуальным геймпадом через `InputSystem.AddDevice<Gamepad>()`; на живом устройстве не проверялось.
- **Присед переключателем, проскок кнопки из меню** (07.09). Пользователь: закрыл терминал на B и присел, потому что B держится и присед был на удержании. `PlayerController`: `_wantCrouch` переключается по `WasPressedThisFrame`, встать можно только если `CanStand()`. `GameManager.UiBlocking` после снятия блокировки держит её ещё один кадр (`_uiReleaseFrame`), чтобы нажатие, закрывшее меню, не дошло до геймплея при любом порядке Update.
- **Меню настроек** (07.09). `Settings` (static, PlayerPrefs `settings.*`, отдельно от сейва): музыка, эффекты, чувствительность X/Y, вибрация; `Sfx.Volume` берётся из эффектов, `PlayerController` умножает look на чувствительность. `UiFactory.Slider` без спрайтов (плоские Image). `UiRoot.BuildSettings`, открывается из паузы, закрывается «Назад»/B (обратно в паузу) или Esc/Start (в игру). Фокус обобщён с `Button` на `Selectable`: влево-вправо двигает ползунок на 5% и переключает вибрацию. Музыка и вибрация пока только хранятся, применять некуда.
- **Локализация: перечитывание текстов** (07.09). Симптом у пользователя: часть UI по-русски, часть по-английски при системном en-US. Причина: таблица грузится асинхронно, тексты, построенные в `Start` до `Loc.Ready`, брали русский fallback и не обновлялись. Сделано: `Loc.Ready` стало свойством, при готовности стреляет `Loc.Changed`; `GameManager` пробрасывает `LocalizationSettings.SelectedLocaleChanged` в `Loc.NotifyChanged`; `UiRoot.Bind(text|button, key, fallback)` регистрирует статичные подписи и перечитывает их в `OnLocChanged` вместе с `RefreshStats/Inventory/Terminal/Vibration`; `RackController` и `PriceTag` подписаны на `Loc.Changed`. Автовыбор языка по системе уже был настроен (`SystemLocaleSelector`, запасной en). В редакторе язык можно подменить меню Locale в Game View (пакет Localization), это не баг.
- **Коллекционки: канистры** (07.09). `Collectible` (Index), `LevelLayoutData.CollectiblePrefab/Collectibles`, `CarModelGenerator.GenerateCanister` (меню 3e, меш из боксов и цилиндра, материал `Cars`), `ScatterBaker.BakeCollectibles` (меню 5b: машинки из раскладки как кинематические препятствия, канистры летят фонтаном ×1,3 скорости, `Scatterer.LaunchBody`). `GameManager.SpawnCollectibles/Collect/ApplyCollectiblesMask`, `CollectiblesMask` в сейве на месте прежнего `CollectiblesFound` (тот всегда был 0 и при загрузке не читался, версия сейва не менялась). `PlayerInteraction.HoverCollectible` с контуром, ЛКМ подбирает, сообщение через `Messages`. HUD-подпись «Коллекция» → «Канистры».
- **Звук подбора канистры** (07.09). `GameConfig.CollectibleClip`, играет через `Sfx.Play` в `GameManager.Collect` в точке канистры. Клип-заглушка `Assets/_Game/Audio/collectible_pickup.wav` синтезирован скриптом (880 → 1320 Гц, ~0,45 с). `ShelfCompleteClip` в конфиге по-прежнему пустой: аудиофайлов в проекте до этого не было.
- **Аудио-заглушки и хуки** (07.09). `Sfx` переписан: пул из 12 `AudioSource`, `Play(clip, pos, vol, pitch)` 3D и `PlayUi(clip)` 2D, громкость из настроек. `MusicPlayer` на объекте GameManager, петля `MusicClip`, громкость `Settings.MusicVolume`. Поля клипов в `GameConfig` (см. GDD раздел 1), пустое поле = тишина. Хуки: `ShelfController.OnArrived/Bounce`, `PlayerInteraction.Take/Throw`, `PlayerAbilities` активации и `NotReady`, `UiRoot` смена фокуса и покупка, `UiFactory.Button` клик, `PlayerController.Footsteps`. Заглушки синтезированы python-скриптом (stdlib wave): place_car, shelf_complete, pickup_car, footstep_walk/run, ui_move, purchase, music_lofi_loop (Cmaj7–Am7–Fmaj7–G7, пентатоника, 22 кГц, бесшовный стык). Пустыми оставлены Bounce, Throw, Ability, AbilityNotReady, UiClick по решению пользователя.
- **Радио и второй трек** (07.09). `GameConfig.MusicClips[]` вместо одного клипа, `MusicPlayer.Play(clips, index)/Next()`, выбранный трек в `Settings.MusicTrack`. `Radio` компонент-маркер, `PlayerInteraction.HoverRadio` с контуром, ЛКМ переключает и показывает «Радио: N/M». `LevelBuilder`: тумбочка и радио из примитивов у южной стены на x = −RoomX/6 (зеркально терминалу), материалы `Cabinet`, `Radio`. Второй трек `music_mall_jingle.wav`: F–Dm–B♭–C, 112 BPM, стабы «электропиано», шагающий бас, щётки на слабых долях, пентатонический мотив, 34 с, бесшовный стык.
- **Вибрация, звук готовности способности, FOV при беге** (07.09). `Rumble` (static): `Pulse(low, high, duration)` через `Gamepad.current.SetMotorSpeeds`, `Tick()` из `GameManager.Update` гасит по таймеру, уважает `Settings.Vibration`. Пресеты: полка, смена фокуса, клик кнопки, готовность способности; ползунки без вибрации. `PlayerAbilities.Update` ловит переход отката через ноль и играет `AbilityReadyClip` (заглушка `ability_ready.wav`) + вибрация. `PlayerController.UpdateFov`: `SmoothDamp` к базовому FOV + `SprintFovBoost` пока бежит и движется. Вибрация вживую не проверена (виртуальный пад моторов не имеет), в WebGL под вопросом.
- **Вибрация не для мобилок** (07.09). `Platform.IsMobile` (по умолчанию `Application.isMobilePlatform`, сеттер под SDK). `UiRoot.OpenSettings` скрывает строку «Вибрация», фокус пропускает неактивные элементы (`Selectable_`: interactable и activeInHierarchy). В WebGL на телефоне `Application.isMobilePlatform` = false, поэтому до SDK строка там будет видна.

## Мобильное управление и UI (07–09.09)
Итог: тач-схема реализована и доведена по тестам пользователя в редакторе (эмуляция `SortThem/Dev/Emulate Mobile UI`). Стик, свайп-обзор, сетка кнопок 2×3 у правого нижнего края, колесо инвентаря на 5 строк, пауза, тапаемые плашки способностей, safe area, масштаб по высоте, заглушка портрета, подавление мыши в тач-режиме. Кнопки бега, приседа и способностей появляются только после покупки апгрейдов. На реальном телефоне и в WebGL не проверялось.
- **Тач-управление** (07.09, ночь, по заданию на утро). `TouchInput` (static): `Move`, `AddLook/ConsumeLook`, `SprintToggled`, `Press/Consume(TouchButton)` с меткой кадра (нажатие живёт 1 кадр, чтобы не зависеть от порядка Update и не стрелять после паузы). `TouchStick`, `TouchLookArea` (правая половина, дельта/высота экрана × `TouchLookSpeed`), `TouchPressButton` (нажатие по PointerDown, подсветка, режим переключателя для бега). `UiRoot.BuildTouchControls` строит слой при `Platform.IsMobile`, `ApplyMobile`: инвентарь поднят, способности по центру и тапаются, подсказки скрыты. Подмешано в `PlayerController` (курсор свободен, мышь не крутит, стик складывается с `Move`, бег-переключатель, прыжок, присед), `PlayerInteraction`, `PlayerAbilities`, `UiRoot` (пауза). Иконки `tc_*.png` нарисованы, бег и присед переиспользуют иконки апгрейдов. Переключатель `SortThem/Dev/Emulate Mobile UI` (EditorPrefs, читается `Platform.IsMobile` в редакторе) оставлен ВКЛЮЧЁННЫМ для утреннего теста.
- **Тач: правки по утреннему тесту 08–09.09.** Баг «способности 2 и 3 не тапаются»: область обзора (правая половина) лежала поверх HUD и ловила касания по плашкам правее центра; тач-слой теперь первый ребёнок контейнера, под HUD. Стрелки инвентаря заменены колесом `InventoryWheel` (5 строк, активный в центре, зацикленный drag с шагом по полстроки, докрутка, тап по соседней строке, без дублей при 1–4 предметах; звук `UiMoveClip` на шаг). Блок кнопок сдвинут на +40/−40, «Бег» и «Присед» 96 px. Иконки: прыжок без подставки, ладони в профиль со стрелками вниз-на-ладонь / вверх-от-ладони. Стик круглый (`circle.png`). `SafeAreaFitter` на контейнере `Safe` (HUD + Touch), меню остаются по центру. CanvasScaler match 1 (высота) в `UiRoot.Awake`/`Start` и `LevelBuilder`. Портрет: `RotateOverlay` с ключом `msg.rotate_device`.
- **Тач: раскладка кнопок по пользователю** (09.09). Пользователь переставил кнопки в Play Mode: два ряда, правый столбец «Взять»/«Бросить» по 120, средний «Прыжок» (100) сверху и «Бег» (96) снизу, левый «Присед» (96) снизу. В коде это сетка по центрам: отступ 60 от края, зазор 24, ряды по центрам 120 и 264, столбцы по центрам −120/−254/−376 (`Centered`). Колесо инвентаря и счётчик остались на месте.
- Колесо инвентаря уменьшено на ~30% по просьбе пользователя (09.09): 266×140, строка 28, шрифты 17/13, счётчик 24; случайные тапы при повороте камеры.
- Тач-режим: `PlayerInteraction.Pressed` отбрасывает нажатия ЛКМ/ПКМ (устройство `Mouse`), чтобы клик по экранной кнопке не дублировался действием ПК; геймпад и клавиатура работают (09.09).

## Как продолжить
Прочитать этот файл, найти последний незакрытый пункт в разделе «Журнал», продолжить с него.

## Решения без пользователя
(решения, которых нет в GDD; на утверждение утром)
- Данные: `CarItemData.Category` и `RackData.Category` хранят ссылку на `CategoryData`, а не строку; `CategoryID` доступен как свойство. Строковый ID остаётся в `CategoryData.CategoryID`.
- У всех SO есть `DevName` (англ. строка): имя для ассетов и запасной текст, если Localization не инициализировалась (например, при сбое Addressables в WebGL). Игра не зависит от локализации критически.
- Один материал на все машинки: цвет через vertex color, шейдер `SortThem/VertexColorLit` (SRP Batcher-совместим). Атлас текстур не нужен для примитивов.
- Контур наведения и зелёный силуэт: один служебный объект (`MeshGhost`) на каждый эффект, ему подставляется меш машинки. Шейдеры `SortThem/Outline`, `SortThem/Ghost`.
- Подсветка дубликатов заменена 06.09 левитацией с контуром `SortThem/OutlineXRay`; шейдер `HighlightThroughWalls` остался в проекте без использования.
- Способности: порядок и параметры пересмотрены 06.09 по референсу, актуально в GDD раздел 5 (1 = поиск совпадений с левитацией, 2 = автосбор, 3 = подсветка стеллажа; 30/60, 10/60, 30/60 с).
- HUD «машинок расставлено» считает только машинки на валидных полках; «полок закрыто» — только валидные полные.
- Чужая модель на занятой полке: если брошена, отскакивает от полки (импульс наружу+вверх); если игрок целится ПКМ в такую полку, действие = бросок.
- Инвентарь хранит машинки списком (не стеками), объект машинки в руках выключен (SetActive false).
- Слот выбирается по порядку заполнения (с 06.09 по референсу), точка прицела на полке не влияет.
- Сейв: бинарный формат → Base64 в PlayerPrefs, позиции квантованы до 5 мм (int16), поворот 4×int8. При несовпадении числа машинок сейв игнорируется.
- Машинки не отбрасывают тени (нет ShadowCaster pass): 1000 теневых кастеров дорого для WebGL.

## Журнал
### Этап 0. Подготовка
- [x] Пакет Localization 1.5.13 установлен, TMP Essentials импортированы
- [x] Слои LooseItems(8) / StaticPlaced(9) / Player(10). Матрица столкновений не менялась: все слои сталкиваются
- [x] Папки Assets/_Game/{Scripts/Runtime,Scripts/Editor,Data,Prefabs,Scenes,Art,UI,Input,Localization}
- [x] Платформа уже WebGL, качество WebGL = Mobile_RPAsset, SRP Batcher включён
- [x] Тестовая WebGL-сборка. Первая попытка через MCP зависла: препроцессор сборки (путь `PreparePlayerBuild` → Addressables `PrepareForPlayerbuild`) показал модальный диалог `DisplayDialog`, главный поток редактора встал, MCP и AppleScript были недоступны. Редактор завершён по SIGTERM (всё было сохранено), сборка перезапущена в batchmode: `Unity -batchmode -nographics -quit -executeMethod SortThem.Editor.BuildTool.BuildWebGL` (в редакторе то же самое — меню `SortThem/Build WebGL`, но там диалог придётся закрыть руками). Результат: Succeeded, 393 с, 0 ошибок, 0 предупреждений, `Builds/WebGL` 18 МБ на диске (data 6.6 МБ, wasm 11.1 МБ, Brotli), Addressables в StreamingAssets/aa. В браузере не проверена: Brotli требует сервер с заголовком Content-Encoding или Build & Run из редактора. `Builds/` в .gitignore.

### Этап 1–3 (код написан, проверка в Play Mode идёт)
- [x] Runtime asmdef `SortThem.Runtime`, editor asmdef `SortThem.Editor`. Оба компилируются без ошибок.
- [x] SO: CategoryData, CarItemData, ShelfData, RackData, EconomyConfig, UpgradeData, GameConfig, CarCatalog, LevelLayoutData.
- [x] Core: CarInstance (Loose/Held/Placed), ShelfController (Empty/Assigned/Reset, отскок, магнит при авторасстановке), ShelfZone, RackController, PriceTag (белый/красный/золотой), EconomyService, UpgradeService, GameManager, CarSpawner, PhysicsActivation, Scatterer.
- [x] Player: PlayerController (WASD/стик, мышь/стик, прыжок, спринт, присед), PlayerInteraction (ЛКМ взять, ПКМ поставить/бросить, скролл), Inventory, PlayerAbilities (1/2/3), MeshGhost (контур и зелёный силуэт).
- [x] UI: UiRoot строит HUD, терминал апгрейдов, меню паузы в рантайме (TMP). UiFactory.
- [x] Save: ISaveStorage, PlayerPrefsSaveStorage, SaveService (бинарный формат, триггеры: закрытие полки, покупка, таймер, потеря фокуса, кнопка).
- [x] Шейдеры: VertexColorLit, Outline, Ghost, HighlightThroughWalls.
- [x] Editor: LocalizationSetup (en/ru, таблица Game), InputSetup (SortThemInput.inputactions, 13 действий, 2 схемы), CarModelGenerator (100 моделей / 10 категорий, ~1100 вершин в среднем), UpgradeSetup (9 апгрейдов), LevelBuilder (зал 36×30×5, 20 стеллажей × 5 полок × 10 слотов = 100 полок, подиум, терминал, игрок, UI), ScatterBaker (Physics.Simulate в edit mode).
- [x] Сцена `Assets/_Game/Scenes/Main.unity` собрана, раскладка 1000 машинок запечена в `Data/LevelLayout.asset`.
- [x] Дымовой тест в Play Mode (кодом через MCP): 1000 машинок спавнятся кинематическими, сфера активации переводит ближние в динамику (145 в радиусе 3 м у кучи), взять/поставить, ценник белый/красный/золотой, +1/−1, закрытие полки (+10, событие), невалидная полка 0, покупки (дальность, инвентарь 5→7), авторасстановка через реальный триггер (брошенная встала в слот), сейв 11 КБ и полная загрузка после перезапуска (11 расставлено, 1 закрыта, баланс, инвентарь, апгрейды).
- [x] Скриншоты: куча читаемая, силуэты и цвета различимы; вывески и ценники читаются; зелёный силуэт в слоте работает.
- [x] Тесты EditMode `Assets/_Game/Tests/EditMode` (экономика, апгрейды, правила полки): 14/14 прошли.

### Dev-инструменты (не игровые фичи)
- Чит: клавиша H добавляет $1000. Только в редакторе и Development Build (`#if UNITY_EDITOR || DEVELOPMENT_BUILD` в `GameManager.Update`), в релизной сборке кода нет.
- `SortThem/Dev/Tick Play Mode In Background` (BackgroundPlayTicker): без фокуса окна редактор не крутит игровой цикл даже с Run In Background; тикер вызывает `EditorApplication.Step()` на каждом editor update, пока приложение не в фокусе. Нужен только для автономной работы через MCP. Утром можно выключить галочкой в меню. Побочный эффект: в этом режиме редактор показывает Play как «paused» между шагами.
- Player Settings → Run In Background включён (для WebGL это поведение при потере фокуса вкладки).

### Известные шероховатости
- LevelBuilder: сразу после сборки сцены ссылки на ассеты в памяти GameManager обнулялись (в файле сцены GUID верные). Лечится `EditorSceneManager.OpenScene` после Build. Причина не найдена, воспроизводится только в одной сессии execute_code.
- Встроенные меши `Cylinder.fbx`/`Sphere.fbx` имеют диаметр 2; используются `New-Cylinder.fbx`/`New-Sphere.fbx`.
- Отскок чужой модели проверен только прямым вызовом (скорость применяется), не физическим броском.
- Бросок машинки своей категории, но другой модели, в начатую полку при купленной авторасстановке: пользователь проверил вживую 07.09, работает ожидаемо.

## Слот-машина (09.09, вечер)
Итог: слот-машина по описанию референса (Quack Pot) спроектирована (решения в GDD раздел 6) и реализована в объёме прототипа. Проверено кодом в Play Mode: спин списывает $100 и даёт одну награду из пула, пул 11 наград вычерпывается до «Пусто», кнопка гаснет, спин на пустом пуле невозможен; после полного пула вместимость +5, откат 60 → 42 с, радиус автосбора ×1,6; золотые иконки в HUD и терминале, золотое число инвентаря. Скриншоты: `Docs/screens/slot_panel.png`, `slot_empty.png`, `terminal_slot_bonus.png`, `slot_machine_world_1.png`.
- Кап терминала по инвентарю: 10 уровней, 5 → 15 (`UpgradeSetup`, ассет `upg_inventory` пересобран меню `3c`). Уровни выше 10 в старых сейвах клампятся `UpgradeService.SetLevel`.
- Новые `UpgradeKind` в конец enum: `InventoryOverCap` (5 уровней, +1 слот), `AbilityCooldown` (×0,9/0,8/0,7 на все три способности), `AutoCollectRadius` (×1,2/1,4/1,6). Поле `UpgradeData.Source` (Terminal/Slot): терминал строит строки только для Terminal, автомат крутит Slot. Ассеты `slot_inventory`, `slot_cooldown`, `slot_radius`, цены нули. Сейв не менялся: уровни пишутся по `Kind`.
- `UpgradeService.SlotRemaining / CanSpin / TrySpin(cost)`: случайная награда среди оставшихся уровней, каждая оставшаяся равновероятна. `Inventory.Capacity` = база + терминал + слот. `PlayerAbilities.CooldownTotal` умножает на множитель отката, автосбор умножает радиус; `IsBoosted(i)` для золотой индикации.
- Объект `SlotMachine` (маркер как `UpgradeTerminal`): куб с золотым экраном, рычагом и надписью CAR POT; наведение обводится контуром, ЛКМ открывает панель. Место: в проёме южной стены рядом с терминалом (проёмы по 2,28 м, свободного места под второй объект у стен нет), терминал сдвинут с x = RoomX/6 на −0,57, автомат на +0,58. Меню `SortThem/4b. Add Slot Machine` пересоздаёт объект в текущей сцене; полная сборка `4` тоже его ставит.
- Панель `UiRoot.BuildSlot`: заголовок, баланс, «Закрыть», барабан (иконка + текст), «Осталось наград: N» / «Пусто. Все награды выданы», кнопка «Крутить · $100». Награда списывается в момент нажатия, 1,2 с барабан перебирает названия из пула, затем показывает выпавшее золотом, вибрация, сообщение внизу «Выпало: …». Геймпад: фокус на кнопке, B закрывает, Start закрывает. Поля `GameConfig.SlotSpinCost`, `SlotLeverClip`, `SlotReelClip`, `SlotWinClip`; заглушки синтезированы и назначены (`Audio/slot_lever.wav` толчок и возврат рычага, `slot_reel.wav` тики каждые 70 мс под шаг барабана, `slot_win.wav` арпеджио C–E–G–C). Сохранение на спин убрано по замечанию пользователя: автосохранения решаем на этапе SDK. Рычаг сначала торчал на 14 см за корпус и сливался со стеллажом, перенесён на передний правый угол.
- Терминал: в колонке уровня золотая подпись бонуса слота («+5», «откат ×0,7», «радиус ×1,6»), иконка строки золотая. Иконка `up_cooldown.png` (секундомер) нарисована заново, генератор иконок в скратчпаде пропал после перезагрузки и переписан.
- Строки локализации `ui.slot*`, `msg.slot_reward`, названия трёх наград в таблице Game (RU/EN).
- Не сделано: бомбы (бэклог, конец), достижения (решено не делать), тест геймпада на автомате вживую.
- **Потеря сейва пользователя (09.09).** Перед тестом я скопировал `sortthem.save` в другой ключ PlayerPrefs, после теста восстановил из него, а ключ оказался пустым; причина не установлена (14-КБ ключи пишутся, `DeleteAll` в коде нет). Восстановил из пустого без проверки, прогресс в редакторе стёрт: 77 машинок, 1 полка, 1 канистра. Баланс $679 и уровни апгрейдов восстановлены кодом по логам теста. Урок: копии сейва только в файл на диске и сравнивать длину перед восстановлением. Ещё один аргумент за резервную копию в самой игре (GDD раздел 8, отложено до SDK).

## Касса (09.09, вечер)
- Стойка `CashDesk` у восточной стены (x ≈ 10,25, z = 0, между стеллажами emergency и construction), на ней аппарат `CashRegister` (маркер как у терминала) с экраном и клавиатурой, развёрнут в зал. Меню `SortThem/4c. Add Cash Register`, полная сборка `4` тоже ставит.
- Логика в `GameManager.ClickRegister`: скрытый счётчик кликов (сообщение «Касса: N/10» убрано по замечанию пользователя: это пасхалка), на `RegisterClicksRequired` (10) выплата `RegisterPayout` ($10) одним платежом и сообщение, флаг `RegisterPaid`. После выплаты клики игнорируются молча (было «Касса пуста», убрано), контур наведения не рисуется. Счётчик сбрасывается, когда прицел уходит с аппарата (`PlayerInteraction.Scan` сравнивает наведение с прошлым кадром, `ClearHover` тоже сбрасывает). Решение пользователя 09.09: не $1 за клик, как в референсе, а $10 за десять кликов подряд.
- Сейв: байт флагов дописан в конец данных (`RegisterPaid` бит 0), при чтении берётся только если байты остались, старые сейвы читаются без версии. Выплата сейв не пишет (автосохранения решаем на этапе SDK).
- Звуки-заглушки `Audio/register_click.wav` (щелчок клавиши) и `register_pay.wav` (звонок и ящик), поля `RegisterClickClip`, `RegisterPayClip`. Сообщения `msg.register_*` RU/EN.

## Бомбы из слот-машины (09.09, вечер)
- `Bomb` (Runtime/Core): фитиль и огонёк укорачиваются по `FuseLeft`, шипение через свой `AudioSource` на префабе, `Hold` (кинематика, коллайдер выключен, родитель камера, поза `BombHandPosition/Euler` из конфига) и `Release` (бросок). Префаб `Prefabs/Bomb.prefab` из примитивов: сфера 8 см, `FuseRoot` с наклоном 25°, цилиндр-фитиль, сфера-огонёк на Unlit; меню `SortThem/3f. Generate Bomb`, назначает `GameConfig.BombPrefab`.
- `GameManager`: `Bombs`, `HeldBomb`, `SpawnBomb` (над случайной лежащей машинкой, +1,5 м), `PickBomb` (взять и поджечь), `ThrowBomb`, `ExplodeBomb` (лежащие машинки в радиусе: `Unfreeze` + `AddExplosionForce` в режиме VelocityChange, множитель ×0,3 если в руке), вспышка корутиной `BombFlash` (растущая сфера 0,2 с на Unlit-материале, создаётся в рантайме).
- `PlayerInteraction`: `HoverBomb`, ЛКМ берёт (приоритет выше канистр и машинок), ПКМ при бомбе в руке бросает бомбу вместо машинки, контур при наведении, пока бомба не в руке.
- Автомат: за спин один исход. При непустом пуле с шансом `BombChance` выпадает бомба вместо награды (деньги списаны, пул не тронут); при пустом пуле спин за $100 всегда даёт бомбу, кнопка не гаснет (решение 09.09, вариант «б»). Первая версия добавляла бомбу к награде («+ бомба»), пользователь поправил: одновременно два исхода не выпадают. Иконка `up_bomb.png` в барабане (`UiRoot.BombIcon`).
- Сейв: байт числа неподобранных бомб после байта флагов, читается только если байты остались; при загрузке бомбы появляются в куче заново.
- Звуки-заглушки `Audio/bomb_fuse.wav` (петля шипения 0,95 с) и `bomb_explode.wav` (хлопок с низким ударом). Строки `ui.slot_bomb*`, `ui.slot_empty_bombs`, `msg.bomb_spawned` RU/EN.
- Проверено кодом в Play Mode: спавн над кучей и падение, подбор в левую руку (родитель камера, коллайдер выключен, шипение играет), взрыв в руках по фитилю уничтожает бомбу; бомба, положенная в кучу и взорванная: из 33 лежащих машинок в радиусе 1,2 м 20 сдвинулись больше 15 см, 18 ещё двигались через 3 с, 5 спящих кинематических проснулись. Скриншот `Docs/screens/bomb_in_hand.png`. Позу в руке придвинул: `BombHandPosition` z 0,7 → 0,55, на 0,7 ядро выглядело мелким.
- Открыто: найти чёрное ядро в куче из 1000 машинок трудно, подсветки у бомбы нет (решить по тестам). Тач: те же кнопки взять/бросить, отдельно не проверялось.

## Баланс наград слот-машины (09.09, вечер)
- Вопрос пользователя: откуда цифры. Категории наград из описания референса, цифры мои. Обнаружено: база `AutoCollectRadius` 15 м покрывала весь зал 22 × 16, награда «радиус» не работала. Решение пользователя: база 8 м, награда ×1,25 / 1,5 / 1,75 → 10 / 12 / 14 м, перепроверить на боевом количестве машинок.
- Новая награда `AbilityDuration` («Долгое действие», `slot_duration`): поиск совпадений и подсветка стеллажа 30 с → 40 / 50 / 60 с (множители 4/3, 5/3, 2), автосбор не трогает. Пул 14 наград. `PlayerAbilities.ActiveTotal` умножает базу, активация берёт длительность оттуда же; золотая индикация и подпись «действие ×N» в терминале. Иконка `up_duration.png` (песочные часы).

## Перемешать кучу (09.09, вечер)
- `GameManager.ShuffleLoose` (корутина): лежащие машинки перемешиваются по порядку и заново вылетают из `Scatterer` тем же кодом, что и запечка (`LaunchCar`), бомбы через `LaunchBody`. Физика в режиме `Script`: `ShuffleCarsPerStep` (4) машинки на шаг `Physics.Simulate(fixedDeltaTime)`, `ShuffleStepsPerFrame` (12) шагов на кадр, выход по `AllSleeping` (проверка каждые 25 шагов) или `ShuffleMaxSteps` (1500). После: машинки за границами в центр, остальные `Freeze()`; режим физики возвращается в `finally`. Левитирующие и удержанные не трогаются.
- `UiRoot`: кнопка «Перемешать кучу» в паузе (панель 428 → 500), затемнение `Fade` (CanvasGroup поверх всего, 0,3 с туда, 0,4 с обратно) с текстом «Перемешиваем кучу… N%»; на время затемнения `UiBlocking` через `Fading` в `AnyOpen`. Строки `ui.shuffle`, `ui.shuffling`.- Проверено кодом в Play Mode (1000 машинок, из них 999 лежат): все 998 сдвинулись больше 0,5 м, за границы никто не вылетел, 903 заморожены (остальные разбужены сферой активации у игрока), режим физики вернулся в FixedUpdate. Длительность перемешивания 0,44 с за 49 кадров, худший кадр 15 мс (редактор, M2); в WebGL ожидаю в несколько раз дольше, затемнение это скроет. Кнопку с геймпада и на телефоне не проверял.

## Язык и меню «Управление» (09.09, вечер)
- `Settings.Locale` (`settings.locale`), `GameManager.InitLocalization` после инициализации таблиц ставит сохранённую локаль, если она есть; иначе остаётся `SystemLocaleSelector`. Строка «Язык» в настройках: кнопка по кругу по `AvailableLocales`, подпись `CultureInfo.NativeName` с заглавной («Русский», «English»), геймпад: влево/вправо тоже листает. Панель настроек 510 → 572.
- `ControlHints` (Runtime/UI): `Label(action, group)` берёт первый биндинг схемы (композиты целиком: «W/S/A/D»), `GetBindingDisplayString`; поле `Glyphs` (TMP_SpriteAsset) для будущих глифов: имя спрайта `устройство_путь` в нижнем регистре, при наличии возвращается тег `<sprite>`.
- Панель «Управление» (`BuildControls/RefreshControls`): 12 строк по таблице `ControlRows`, строка выбора предмета особая (колесо мыши / LB RB), перестраивается при смене устройства и языка. Кнопка в паузе между «Настройки» и «Вернуть застрявшие», скрыта при `TouchInput.Active`; высота паузы теперь считается по числу видимых кнопок.- Проверено в Play Mode: справка строит 12 строк («[W/A/S/D] Move», «[Space] Jump», «[LMB] Take», «[Mouse wheel] Select item…»), строки для геймпада без устройства: LS/RS, A, B, RT/LT, LB/RB, D-Pad/Up, Start (имена из Input System; на DualSense будут свои). «Delta» у обзора заменено на «Мышь». Переключатель языка: Русский ↔ English, заголовки и HUD сменились сразу, `settings.locale` записался; после теста сброшен в пустое, чтобы остался режим первого запуска. Скриншоты `Docs/screens/controls_help.png`, `settings_language.png`.
- 10.09: по замечанию пользователя кнопки «Язык» и «Вибрация» с геймпада больше не листаются стиком влево/вправо (выглядят как кнопки без стрелок), переключаются только подтверждением (Submit / A), как любая кнопка. Стик влево/вправо остался только у слайдеров.- 10.09: подписи клавиш на плашках способностей («1 2 3» / «↑ ← →») и строка подсказки сверху HUD больше не захардкожены: берутся из биндингов через `ControlHints.Short` (короткая форма для тесных мест: d-pad стрелками, Left Shift → Shift, Left Control → Ctrl, Left Stick Press → LS). Подсказка стала форматной строкой `ui.hint` с {0}–{3} и перестраивается при смене устройства и языка.


## Обучение (10.09, ночь)
- `Tutorial` (Runtime/Core, объект `Tutorial` в сцене, создаёт `LevelBuilder.BuildTutorial`, меню `4d. Add Tutorial`, входит и в `4. Build Level Scene`): шаги `Walk → Look → Take → Place → Done`, статические `Running/BlocksInteract/BlocksPlace`. Стартует после `gm.Ready`, если `ForceRun || !gm.TutorialDone`. Ходьба: сумма горизонтального смещения игрока ≥ `WalkDistance` (2 м); обзор: сумма `Quaternion.Angle` по `CameraPivot` ≥ `LookDegrees` (90°); в паузе (`UiBlocking`) счётчики не растут. Цель «взять»: ближайшая Loose-машинка, у которой рейкаст сверху попадает в её же коллайдер; контур `MeshGhost` с материалом `OutlineYellow` (SortThem/OutlineXRay, ширина 0,02). «Поставить»: `RackController.SetHighlight` у стеллажа категории активной машинки, финиш по росту числа Placed-машинок; пустой инвентарь без постановки — назад к «взять».
- Блокировки: `PlayerInteraction.Update` не вызывает Interact/PlaceOrThrow/Next/Prev/скролл по флагам туториала, в `Scan` при `Tutorial.Running` обнуляются наведения на терминал, автомат, кассу, радио, коллекционки, бомбы; `PlayerAbilities` не активирует способности пока `Tutorial.Running`.
- Сейв: флаговый байт в хвосте теперь `bit0 RegisterPaid | bit1 TutorialDone`, формат/версия без изменений. На старых сейвах бит нулевой, обучение покажется один раз (у пользователя тоже).
- UI (`UiRoot.BuildTutorial/UpdateTutorial/TutorialTransition/TutorialText`): панель 500×52 под балансом (y −116, ниже строки «Сохранено»), чекбокс из спрайта `SlotFrame`, галочка из двух зелёных полосок, текст с автоподбором 15–22 pt. Переход: галочка 1 с → fade 0,3 с → новый текст → fade in; при откате к «взять» галочки нет. Ключи `tut.walk/look/take/place` (+`_touch`), клавиши через `ControlHints.Label`, обзор на клавиатуре — `ctl.mouse_key`. На таче `UiPulse` (масштаб 1,0–1,12) на стике для «походить», кнопке «взять», кнопке «поставить».
- Проверено в Play Mode кодом: клавиатура («W/A/S/D — walk around»), виртуальный геймпад (LS, RS, RT, LT), тач-эмуляция (тексты про стик/кнопки, пульсация переключается по шагам и гаснет), русская локаль (длинные строки ужимались до 16 pt, тач-строки укорочены). Полный проход: галочки, подсветка стеллажа cat_retro для машинки cat_retro, финиш, `TutorialDone=true`, сейв записан, панель скрыта. Ошибок в консоли нет. Скриншоты `Docs/screens/tut_walk.png`, `tut_tick.png`, `tut_target.png`, `tut_touch.png`, `tut_touch_take.png`, `tut_touch_ru.png`.
- Побочный эффект теста: в сейв пользователя добавилась одна поставленная машинка (cat_retro), бит `TutorialDone` уже стоит. Копия сейва до теста: scratchpad `save_backup_tutorial.txt` (14 728 символов), по договорённости не восстанавливалась.

## Стресс-тест 4000 машинок (10.09, ночь)
- `ScatterBaker.Bake(int copiesPerModel)`: перегрузка для запекания с другим числом копий на модель (меню осталось `Bake()` = ёмкость полки, 10). Запечено 40 копий × 100 моделей = 4000 (6000 шагов, до конца не заснули, при загрузке всё равно кинематика). `LevelLayout.asset` сейчас на 4000; копия исходной раскладки на 1000: scratchpad `LevelLayout_1000.asset.bak` (154 536 байт), либо просто перезапустить меню `5. Bake Scatter Layout`. Сейв пользователя с 1000 машинками при 4000 игнорируется («car count mismatch») и перезаписан автосейвом на 4000 (58 736 символов); копия до теста: scratchpad `save_backup_before_4000.txt` (14 728).
- Замеры в редакторе (M2, Game View, профайлер включён, эмуляция мобильного UI была включена пользователем), `ProfilerRecorder`, 120 кадров на точку:
  - спавн 4000 через `CarSpawner.SpawnFromLayout`: 86 мс;
  - у спавна: кадр 3,1 мс, 1279 батчей, 1,25 M треугольников;
  - в куче, взгляд вниз: кадр 4,3 мс, 2573 батча, 2,58 M треугольников, Physics.Simulate 0,4 мс (841 бодрствующее тело после телепорта);
  - из угла на весь зал: кадр 5,5 мс, 4119 батчей, 4,17 M треугольников (каждая машинка — свой батч под SRP Batcher, ~1000 треугольников на примитивную машинку), Physics.Simulate 0,5 мс;
  - `PhysicsActivation.Tick` на 4000: 0,55 мс за вызов (линейный проход с чтением transform.position);
  - «Перемешать кучу» на 4000: 6,7 с синхронного зависания (на 999 было 0,44 с);
  - разбудить 2927 тел в радиусе 6 м: первый кадр 524 мс (Physics.Simulate 75 мс за шаг, догоняющие FixedUpdate), далее 1,9 мс/кадр физики, засыпают за ~7–10 с;
  - тени: машинки теней не отбрасывают (63 кастера в кадре), Shadows.DrawSRPBatcher 0,1 мс.
- Вывод записан в ответе пользователю: узкое место — количество draw call'ов и треугольников, оно целиком зависит от боевых ассетов; физика и скрипты в норме. Скриншоты `Docs/screens/perf_4000_pile.png`, `perf_4000_hall.png`.
- 10.09: по просьбе пользователя `ActivationRadius` 3 → 1,5 м (GameConfig.asset). Замер в центре кучи на 4000, телепорт и 3 с ожидания: r=3 — 677 динамических тел, 675 бодрствуют, Physics.Simulate 0,20 мс; r=1,5 — 152 тела, все спят, 0,02 мс. При 3 м куча вокруг игрока не успокаивалась (тела будили друг друга контактами), при 1,5 м засыпает.

## WebGL-сборка для черновика Яндекс Игр (10.09, день)
- Player Settings: Compression Format Brotli + Decompression Fallback вкл. (файлы `.unityweb`, распаковка в загрузчике, заголовки сервера не важны). Сборка через меню `SortThem/Build WebGL`: успех, 19,1 МБ на диске, 0 ошибок/предупреждений, около 6 минут. Архив `Builds/SortThem_yandex.zip` (index.html в корне) для загрузки черновиком. В сборке раскладка на 4000 машинок (стресс-вариант), SDK Яндекса не подключён (этап 7); черновик, насколько известно, запускается без SDK. `Builds/` в репозиторий не входит.
- 10.09: счётчик FPS в HUD (`UiRoot.UpdateFps`): зелёный жирный текст 24 pt слева под панелью статистики (28, −134), среднее за 0,5 с по `unscaledDeltaTime`, всегда включён. Скриншот `Docs/screens/fps_counter.png`. Замечено на этом скриншоте: при русской раскладке клавиатуры Input System показывает «Ц/Ф/Ы/В» вместо «W/A/S/D» (имена клавиш берутся из текущей раскладки), касается обучения, справки и подсказок; пользователю не сообщалось ранее, решение не принято.
- 10.09, по замечанию пользователя из WebGL-теста («тени на машинках»): машинки тени не отбрасывали и раньше (`m_CastShadows: 0`), но принимали тени стеллажей и стен. Выключено в трёх местах: `m_ReceiveShadows: 0` во всех 100 префабах `Prefabs/Cars`, `CarModelGenerator` ставит `receiveShadows = false`, и в шейдере `SortThem/VertexColorLit` убраны `_MAIN_LIGHT_SHADOWS*`/`_SHADOWS_SOFT` и семплирование карты теней (`GetMainLight()` без shadowCoord), потому что в URP галочка Receive Shadows на рендерере сама по себе шейдер не меняет. Шейдер используется только `Cars.mat`. Проверено в Play Mode: 4000 машинок, receive=0, cast=0, куча без пятен теней (`Docs/screens/cars_no_shadows.png`). Сборку следующего билда пользователь делает сам через `SortThem/Build WebGL`; в этот раз ждать `index.html` нельзя, Unity его не перезаписывает при неизменном шаблоне, поэтому мой фоновый ожидатель завис, остановлен.

## Оптимизация под 4000 без боевых ассетов (10.09, день)
- Решение пользователя: 4000 машинок остаются (как в референсе). Тест WebGL на телефоне: 30 FPS на старте, 4–5 FPS на куче.
- Mobile_RPAsset: `m_MainLightShadowsSupported 1→0`, `m_SupportsHDR 1→0`, `m_RenderScale 0.8→0.75`. Профиль Mobile стоит для WebGL (QualitySettings `WebGL: 0`).
- Шейдер `VertexColorLit`: `#pragma multi_compile_instancing` в обоих проходах, `UNITY_VERTEX_INPUT_INSTANCE_ID`/`UNITY_SETUP_INSTANCE_ID`, `_BaseColor` вне CBUFFER (шейдер вне SRP Batcher намеренно). `Cars.mat`: `m_EnableInstancingVariants: 1`.
- `PhysicsActivation.Tick(cars, center, radius, slices, frame)`: проход по срезу `frame % slices`; GameManager вызывает с 3 срезами.
- Замер в редакторе (4000, те же точки, что утром): спавн 207 батчей (было 1279), куча сверху 202 (было 2573), угол на весь зал 223, из них 111 инстансированных на 3992 машинки (было 4119); треугольники не изменились (2,2–4,2 M), время кадра в редакторе не изменилось (упор в редакторе не в CPU-вызовы). Выигрыш ожидается в WebGL, где стоимость вызова высокая; подтверждение только билдом на телефоне. `PhysicsActivation` 0,3 мс/вызов вместо 0,55. Скриншот `Docs/screens/instancing_hall.png`.
- Новый файл `ASSET_REQUIREMENTS.md`: бюджет 300–600 треугольников (потолок 800), один меш/сабмеш, один материал (вершинный цвет или один атлас без альфы), пивот в центре, размеры болванок, FBX латиницей, бокс-коллайдер, приёмка по Instanced Batches.
- Не делалось: чанки замороженной кучи и ограничение дальности отрисовки, ждём замера на телефоне; колёса/полигонаж болванок не трогали по договорённости.

## Накопление живых тел при ходьбе по куче (10.09, день)
- Симптом пользователя в WebGL на телефоне: 60 FPS на старте, при активной ходьбе по куче FPS падает и не восстанавливается, даже глядя в стену. Воспроизведено кодом: игрок кругом радиусом 3 м со скоростью 2 м/с по куче из 4000, живых (не кинематических) тел 105 → 568 → 843 → 1128 → 1329 за 20 с, через 8 с после остановки 1350 и все бодрствуют, vmax ≈ 0,2 м/с. Причина: `PhysicsActivation` замораживал вне радиуса только `IsSleeping()`, а разбуженные машинки в куче не засыпают (дрожат, порог сна 0,005).
- Правка: `GameConfig.FreezeSpeed` (0,35) и `FreezeDelay` (0,5 с); `CarInstance.CalmSince`; `PhysicsActivation.Tick(cars, center, radius, freezeSpeed, freezeDelay, slices, frame)` вне радиуса замораживает спящие тела сразу, а тела со скоростью ниже порога (и угловой < 2 рад/с) после `FreezeDelay` непрерывного спокойствия; внутри радиуса таймер сбрасывается.
- Тот же прогон после правки: 134 → 276 → 305 → 334 → 311 → 309, через 3 с после остановки 187 живых и 0 бодрствующих. Компиляция чистая, ошибок нет.
- Не сделано: перепечь раскладку 4000 до полного покоя (бейкер остановился на 6000 шагах не заснувшим), это уменьшило бы дрожь в исходной куче; лимит на число живых тел как страховка.

- 10.09: заведён `OPTIMIZATION.md` (сделано с цифрами, что не дало результата, запас без ассетов, план с ассетами, методика замеров); ссылки из PROGRESS, WORK_PLAN, GDD §8.

## Импорт ассетов машинок (10.09, вечер)
- Пользователь импортировал в `Assets/Cars model/` четыре пака: ARCADE - Ultimate Vehicles Pack, Low Poly Cars Pack (Large), Modular_Cyber_Racing_Cars, Stylized Vehicles Pack. У каждого своя папка с демо-сценами. Что с ними делать, пользователь скажет позже.
- Демо-сцена `Modular_Cyber_Racing_Cars/Scenes/URP_Scenes/Demonstration.unity` не работала: `InvalidOperationException` про `UnityEngine.Input` при Active Input Handling = Input System. Источники: `Demonstration_Scripts/Input/StandardInput.cs` (оси и Shift), `CameraController/CameraControllerStandart.cs` (клавиши 1/2) и `StandaloneInputModule` на EventSystem сцены. Правка: оба скрипта переписаны на `Keyboard.current` (WASD/стрелки, Shift, 1/2), модуль EventSystem заменён на `InputSystemUIInputModule`, сцена сохранена. Active Input Handling проекта не менялся. Материалы пака уже URP Lit, конвертация не нужна. Проверено в Play Mode: консоль чистая, скриншот `Docs/screens/cyber_cars_demo-1.png`.
- Внимание: к MCP подключены два инстанса Unity, после перезагрузок домена активный инстанс слетает на чужой проект, и `read_console` показывает ошибки из него (`Assets/Scripts/InteractionSystem/...`, `Assets/100BestEffectPack/...`, в нашем проекте таких файлов нет). Перед чтением консоли переустанавливать `set_active_instance("sort-them@1446a054")`.

## Пул машинок из паков (10.09, вечер)
- По просьбе пользователя изучены три пака (Modular_Cyber_Racing_Cars отложен): метрики 492 префабов сняты кодом в Unity (bounds, треугольники, материалы, рендереры) в scratchpad `car_pool_raw.csv`; 368 миниатюр отрендерены временной камерой, собраны в 16 нумерованных листов-контактов (Python без PIL, свой PNG-кодек), листы в `Docs/screens/car_pool/` (5,6 МБ). Типы кузовов проставлены визуально по листам.
- Результат в `CAR_POOL.md`: сводка по пакам, таблицы по каждой машине (тип, длина, треугольники, материалы, «подходит» по порогу 6,5 м), черновик 10 категорий из ARCADE, выводы. Подходит: ARCADE 103 городских + 129 гоночных, Low Poly 69, Stylized 27.
- Главные находки: полигонаж всех паков 2–12 тыс. треугольников на машину против бюджета 300–600 (нужна децимация при импорте или дальние упрощённые меши); колёса везде отдельными мешами (склейка при импорте); ARCADE с 24 общими материалами и BoxCollider лучший кандидат на базовый набор; Low Poly это «машины из кино» другим стилем; Stylized крупнее на 30% и по 3–7 материалов на машину; `SpecialCar2_combined` без кузова. Уточнение к `ASSET_REQUIREMENTS.md`: для инстансинга важна пара меш+материал, число материалов влияет только на SetPass; правку в файл пока не вносил.
- Unity MCP во время рендера миниатюр ушёл в таймаут и после него не отвечал несколько минут; результат при этом записался полностью.
- 10.09: решение пользователя: база ARCADE. В `CAR_POOL.md` добавлен раздел «Кандидаты по категориям»: 10 категорий, 234 машины без пересечений, по листу на категорию `Docs/screens/car_pool/cat_*.png`. Военные, мотоциклы, прицепы, тягачи, автобусы и длинные грузовики не вошли.
- 11.09: 236 префабов ARCADE скопированы по 15 папкам `Assets/_Game/Art/CarPool/` для отбора пользователем (таблица в `CAR_POOL.md`, раздел «Папки для отбора»). Референс уточнён пользователем: 16 категорий × 10 видов × 20 штук (два соседних отсека по 10 на вид), число предметов у нас примерное, категории и количество решаются после отбора.

## Децимация машинок ARCADE, проба на Alcyone (11.09)
- Blender через MCP-аддон упал на экспорте (контекст без окна), поэтому децимация делается фоновым Blender'ом из командной строки: `Blender -b --python scratchpad/decimate_car2.py -- <fbx> <out_dir> '<json вариантов>'`. Скрипт: импорт FBX, на кузов Decimate Planar (delimit UV/MATERIAL/SHARP/SEAM) + Decimate Collapse с симметрией по X, вершины материала `Lights` защищены весовой группой (invert, factor 10) чтобы фонари не разъезжались; колёса Planar + Collapse; экспорт FBX c FBX_SCALE_ALL (узлы получают scale 0,01, при боевом импорте запечь).
- Результаты на Alcyone (оригинал 4912 треугольников: кузов 2880 + 4 колеса по 508): A 1822 и B 1072 без защиты фонарей (фонари в треугольники, брак), C 2752 и E 2448 только планарная (мало, кузов криволинейный, удалены), D 1774 (planar 5°, collapse 0,5, защита), F 1178 (0,33), G 888 (0,25, колёса 71). FBX в `Assets/_Game/Art/CarPool/_Decimated/`, сцена `Assets/_Game/Scenes/CarCompare.unity`: Original, DecD, DecF, DecG в ряд, OldA/OldB сзади, материалы оригинала назначены по именам слотов. Скриншоты `Docs/screens/decimate_compare-1.png`, `dec_orig.png`, `dec_D.png`, `dec_F.png`, `dec_G.png`.
- Визуально: D почти неотличим, F слегка гранёный кузов, G заметно гранёный и колёса шестиугольные. Решение за пользователем. Если колёса будут мешать, заменить их на чистый 12-гранный цилиндр с UV оригинала вместо децимации.
- Пользователь параллельно чистит папки CarPool до 10 моделей на категорию, брак уносит во внутреннюю папку.
- 11.09, продолжение: пользователь считает даже D (1774) плохим. Сетка оригинала в Blender: кузов чистые квады (1192 квада, 424 треугольника, 12 n-гонов), колёса со спицами по 305 граней = 41% модели. Пробы сохранить квад-структуру: Decimate Un-Subdivide (2 и 4 итерации, кузов 1500/1442) и QuadriFlow remesh (900/500 граней → 1682/854 треугольника) с Data Transfer UV с оригинала. Оба варианта геометрически ровные, но UV разрушены (радужные полосы, `Docs/screens/dec_W1.png`, `dec_W2.png`): Un-Subdivide UV не сохраняет, перенос UV после QuadriFlow не сработал (вероятно, несовпадение имени UV-слоя при mapping NAME, не доведено). Колёса заменены 16-гранным цилиндром с усреднёнными UV покрышки и диска: 60 треугольников вместо 508, выглядит нормально (`decimate_car3.py`, функция `wheel_cyl`).
- Вывод: автоматически получить из этих мешей один лёгкий вариант, неотличимый от оригинала, не выходит; коллапс портит затенение, ремеш теряет UV. Предложение пользователю: LOD вместо одного меша, оригинал вблизи, коллапс-вариант вдали, где артефакты не видны; цилиндрические колёса только в дальнем варианте. Сцена `CarCompare.unity` содержит все варианты (W1, W2, Q1 с битыми UV оставлены для наглядности).

## Скрытие закопанных машинок (11.09)
- Реализовано по идее пользователя: `Runtime/Physics/PileOcclusion.cs` (Init после Ready с `Physics.SyncTransforms`, MarkDirty из `CarInstance.Freeze/Unfreeze/SetHeld/SetLoose`, Tick из GameManager, `CarInstance.SetVisible/Hidden`), конфиг `BuriedCulling`, `BuriedRayLength`, `BuriedSideRays`, `BuriedNeighborRadius`, `BuriedPerFrame`; счётчик скрытых в HUD рядом с FPS при включённом режиме.
- Результат отрицательный на текущей куче, подробности и цифры в `OPTIMIZATION.md` («Пробовали»). Метод проверки: два скриншота с одной камеры и попиксельная разница (`scratchpad/imgdiff.py`, порог 40 по сумме RGB), шум 0,04%. Скриншоты `Docs/screens/buried_*.png`. Выключено по умолчанию (`BuriedCulling = false` в коде и в GameConfig.asset), код оставлен.

## Ресёрч чужого опыта оптимизации (11.09)
- По просьбе пользователя два поисковых агента собрали первичные источники: патчноуты Sort Them Ducks (LOD уткам), девлоги Hyperbole и Robot Shooter, треды команды Unity Physics про острова контактов и timestep, доклад Khronos про SRP Batcher на WebGL, мануал Unity Web, треды Unity Web Graphics (targetFrameRate, WebGPU, BRG), RU-статьи про Яндекс Игры. Сведено в `RESEARCH_OPTIMIZATION.md` с пометками подтверждено/вывод, списком «что уже есть» и десятью пунктами «применимо и не сделано» (timestep 0,033, sleepThreshold, диагностика render scale, бюджет активных тел, ASTC для текстур ARCADE, WebAssembly 2023, Initial Memory Size, чанки как резерв). Ничего в проекте не менялось.

## Импорт боевых машинок ARCADE в игру (11.09)
- Пользователь отобрал по 10 моделей в 15 папках `Art/CarPool/*` (брак в подпапках `stash`). Написан `Editor/ArcadeCarImporter.cs` (меню `3g`): чистит старые `Data/Cars`, `Prefabs/Cars`, `Data/Categories`, `Data/Racks`, `Art/Meshes/car_*`; для каждого префаба ARCADE инстанцирует, собирает CombineInstance по целевым материалам (исходный материал → наш `Arcade_<имя>` на `SortThem/TexturedLit`, `_BaseMap` и `_BaseColor` копируются, инстансинг вкл.), масштаб min(0,075, 0,42/длина, 0,30/высота), центрирование по bounds, меш в `Art/Meshes/Arcade/`, префаб как у болванок (бокс-коллайдер, Rigidbody 0,3, `CarInstance`), `CarItemData` с ценой по старой формуле и локализацией имени (ru = en = имя модели). 150 машинок, 15 категорий, 14 материалов, 36 с; треугольники 2788 (Eos) … 7372 (Oblivion), среднее 4483; длина 0,20–0,42 м, высота до 0,27.
- Новые шейдеры `TexturedLit` (инстансинг, без теней, `_BaseColor` вне CBUFFER как у VertexColorLit) и `TexturedLitOverlay` (вид в руке). `HeldItemView.HeldMaterials`: для материалов TexturedLit создаёт кэшированные overlay-копии, иначе прежний `CarsHeld`.
- `LevelBuilder`: зал 22×16 → 36×20, слоты стеллажей по числу категорий (верх 5, низ 5, лево 3, право 2), терминал/автомат и шкаф/радио в проёмах нижней стены на x = ±10,8 (`ShopGapX`), касса на восточной стене без изменений. Сцена пересобрана меню 4: 15 стеллажей, 150 полок. `ScatterBaker` получил `Physics.SyncTransforms()` перед симуляцией.
- Грабли: первый бейк сразу после пересборки сцены дал 4050 машинок в кубе ±1 м (все спасены к центру: коллайдеры свежепостроенной сцены ещё не были в физике), и этот результат успел уйти в автосейв. После добавления SyncTransforms бейк нормальный (разлёт ±10 × ±9 м, 28 с), сейв стёрт (копия scratchpad `save_backup_broken_pile_4050.txt`).
- Замер в редакторе (M2, 4050 машинок): спавн 8590 инстансов / 16,8 M треугольников / 461 батч; над кучей 5877 / 11,6 M / 441; из угла 9271 / 18,2 M / 486. Против болванок треугольников в 5 раз больше (11,6 M против 2,2 M над кучей), батчей в 2 раза (сабмеши). Кадр в редакторе 6,6–8,2 мс. Проверено: взять/поставить работает, стеллаж категории принимает, вид в руке текстурный. Скриншоты `Docs/screens/arcade_pile.png`, `arcade_racks.png`.
- Не сделано: LOD (следующий шаг после замера на телефоне), две полки на вид, перекрасы как виды, иконки/цвета категорий подобраны наспех в коде импортёра.
- 11.09: по просьбе пользователя тени выключены полностью: Directional Light в сцене `shadows = None` (и в `LevelBuilder` для пересборок), в `PC_RPAsset` главный и дополнительные источники без теней (мобильный профиль уже был без теней). Машинки ARCADE тени не отбрасывали и не принимали, тени давали 427 объектов зала.

## Баг WebGL-черновика: машинка в руке не видна, HUD 0/5, постановка теряет машинки (11.09)
- Причина: `HeldItemView.HeldMaterials` создавал материал через `Shader.Find("SortThem/TexturedLitOverlay")`. Шейдер не упоминался ни одним ассетом, поэтому в WebGL-сборку не попал, `Shader.Find` вернул null, `new Material(null)` кидал исключение внутри `Inventory.Changed`. `HeldItemView` подписан раньше `UiRoot`, поэтому HUD не обновлялся; при постановке исключение вылетало из `Inventory.RemoveActive()` до `TryPlaceAnimated`, машинка удалялась из инвентаря и оставалась в состоянии Held (скрытая), на полку не попадала. Последняя ставилась, потому что при пустом инвентаре вид в руке материал не пересоздавал. В редакторе шейдер есть всегда, поэтому там не воспроизводилось.
- Правка: `HeldItemView.TexturedOverlayShader` (сериализованная ссылка, назначается в `LevelBuilder` и проставлена в текущей сцене), `Shader.Find` убран, при отсутствии шейдера fallback на прежний материал `CarsHeld` без исключения. Файл сцены теперь ссылается на шейдер, сборка его включит. Проверено в Play Mode: 3 машинки подобраны (HUD 3/5, в руке оба сабмеша на overlay-шейдере), 3 поставлены, потерянных 0. Нужна пересборка WebGL пользователем.
- Оставшийся `Shader.Find("Universal Render Pipeline/Unlit")` во вспышке бомбы безопасен: URP Unlit используется материалами вывесок в сцене.
- Побочное наблюдение: повторяющиеся NullReference в `GameManager.Update` (Save.Tick) и `OnApplicationFocus` это следствие перекомпиляции во время Play Mode (домен перезагружается, статики обнуляются), а не баг игры. Правила: не компилировать при запущенном Play Mode.

## LOD машинок ARCADE (11.09)
- Телефон пользователя на 4050 машинках ARCADE без LOD: падение до 14 FPS. Треугольников в кадре было в 5 раз больше, чем на болванках (11,6–18,2 M против 2,2–4,2 M), поэтому первым делом LOD.
- Децимация всех 146 исходных FBX фоновым Blender'ом: `Tools/lod_cars.py` (в репозитории, чтобы не потерять, как скрипты в scratchpad). Запуск: `Blender -b --python Tools/lod_cars.py -- <папка вывода> '<json вариантов>' <fbx...>`; варианты в проекте: `LOD1` = кузов Planar 5° + Collapse 0,5 с защитой материалов, содержащих `light` (весовая группа, factor 10), колёса Planar 6° + Collapse 0,3; `LOD2` = кузов Collapse 0,25, колёса заменены 12-гранным цилиндром с усреднёнными UV покрышки/диска. 146 файлов за ~2 мин, вывод в `Assets/_Game/Art/CarPool/_LOD/<fbx>_LOD1.fbx` и `_LOD2.fbx` (292 файла). Треугольники по Blender: оригинал среднее 4507 (2788…9102), LOD1 1682 (978…3872), LOD2 777 (420…1942).
- `ArcadeCarImporter` (меню 3g): для каждой машинки рядом с основным мешем строит `car_<id>_LOD1/LOD2` из LOD-FBX той же матрицей (масштаб и центр LOD0), материалы подбираются по имени узла в исходном префабе (`Body`, `FrontLeftWheel`, …) и раскладываются в порядке сабмешей LOD0, чтобы `sharedMaterials` рендерера подходили всем уровням; детали FBX, которых нет в префабе (лишние вывески фудтраков, `Vinyls` у одной модели), пропускаются. Проверка габаритов LOD против LOD0 (предупреждение при расхождении >5%, ни одного). Импортёр больше не удаляет ассеты перед пересозданием: категории, данные машинок, префабы и меши перезаписываются на месте (GUID сохраняются, сцена и стеллажи не ломаются), лишние файлы удаляются после. Результат: LOD1 в среднем 1674, LOD2 775 треугольников (после объединения с пропуском лишних деталей).
- Рантайм: свой выбор LOD вместо LODGroup, потому что весь код (обводка, призрак, вид в руке, скрытие, коллайдер) работает с одним `MeshFilter`/`MeshRenderer` на корне машинки. `CarInstance.Lods` (массив мешей на префабе, [0] = оригинал), `CarInstance.Lod` (текущий уровень), `Physics/CarLod.Tick` из `GameManager.Update`: по третям списка в кадр, расстояние от `CameraPivot`, пороги `GameConfig.LodDistances` (3 м, 6 м), гистерезис ±10% чтобы не мигало на границе, машинки в руке пропускаются; смена уровня = замена `sharedMesh`. Инстансинг работает как раньше (группы по мешу и материалу).
- Замер в редакторе (M2, 4050 машинок), LOD выкл → вкл: спавн 16,85 M → 4,00 M треугольников (batches 424 → 885), над кучей 11,55 M → 3,84 M (386 → 927), угол на весь зал 17,83 M → 3,13 M (425 → 508). Распределение над кучей: 766 LOD0, 2136 LOD1, 1148 LOD2. Кадр в редакторе 7,3/5,8/6,9 мс → 4,9/4,7/5,0 мс. Скриншоты `Docs/screens/lod_pile_on.png`, `lod_pile_off.png` (различий на глаз нет), `lod_force_lod2.png`, `lod_force_lod1.png` (принудительно один уровень вблизи, для оценки качества).
- Цена: число батчей выросло в 2 раза (каждый уровень LOD это отдельный меш, групп инстансинга втрое больше: 150 моделей × 3 уровня × 2–3 сабмеша). На WebGL вызовы отрисовки дорогие, это следующий кандидат: атлас текстур (14 материалов → 1) уберёт сабмеши и сократит батчи в 2–2,5 раза и с LOD, и без.
- Пороги разделены по платформе (11.09, просьба пользователя): `GameConfig.LodDistancesMobile` 3/6 м (бывший `LodDistances`, FormerlySerializedAs) и `LodDistancesDesktop` 10/20 м, выбор по `Platform.IsMobile` в `GameManager.Update`. Полностью выключать LOD на ПК не стали: без LOD из угла зала 18 M треугольников, ноутбуки со встроенной графикой в браузере это не тянут, а на 6 м разницы не видно.
- Не сделано: подбор мобильных порогов по замеру на телефоне (первый тест пользователя: LOD помог, но мало, цифр нет), атлас.

## Баг тач-стика: центр даёт максимум вперёд (11.09)
- Причина: подложка стика создаётся `UiFactory.Anchored`, который ставит пивот равным якорю (0; 0), то есть в левый нижний угол; `TouchStick.Apply` считал смещение от пивота, поэтому касание центра подложки давало (120; 120)/80 → максимум «вперёд-вправо», а «назад» требовало касания ниже подложки, куда событие не приходит. Пивот у подложки такой с момента создания стика; тач-версию до этого толком не проверяли на устройстве.
- Правка: `TouchStick.Apply` отсчитывает смещение от `_rect.rect.center`; подложке стика поставлен пивот (0,5; 0,5) с позицией (180; 180), чтобы пульсация туториала шла от центра (место на экране прежнее). Проверка в Play Mode с эмуляцией мобильного синтетическими `PointerEventData`: центр (0; 0), +40 px вверх (0; 0,5), −80 px вниз (0; −1), −120 px влево (−1; 0), диагональ (0,7; −0,7). Пульсация `UiPulse` у стика по-прежнему масштабирует относительно угла (пивот не трогали), при желании исправить пивотом (0,5; 0,5) с пересчётом позиции.

## Баг LOD: машинки белеют вдали (12.09)
- Симптом от пользователя на ПК: красная машинка при удалении становится белой. Не мипмапы и не UV: разброс UV в треугольниках LOD такой же, как в оригинале (проверено по 3 моделям).
- Причина 1: Unity при импорте LOD-FBX из Blender иногда меняет порядок сабмешей (у Europa в LOD2 первым идёт сабмеш `Lights`, у оригинала первым кузов), а импортёр брал материал по индексу сабмеша. Кузов получал текстуру фонарей (почти белая), фонари цвет кузова.
- Причина 2 (после первой правки): у 4 фургонов сабмеш фонарей LOD слился с кузовом, пустой слот `CombineMeshes` выбросил, колёса сдвинулись на индекс фонарей. Голосование по центроидам треугольников для фонарей давало 50:46 в пользу кузова (фонари лежат вплотную к кузову).
- Правка (`ArcadeCarImporter.MatchSubmesh`): сабмеш LOD сопоставляется с сабмешем оригинала по ближайшей *вершине* в пространстве позиция + UV (вес UV 0,1): защищённые вершины фонарей в LOD совпадают с оригиналом точно и по позиции, и по UV, дубли на границе островов различаются по UV. Для пустого слота добавляется вырожденный треугольник, чтобы число сабмешей всегда равнялось LOD0. Проверка по всем 150 машинкам: 300 LOD-мешей, число сабмешей совпадает, ни в одном сабмеше треугольников не стало больше, чем в LOD0 (`suspicious=0`).
- Заодно колёса LOD2: розовые кружки на дисках были из-за усреднения UV по всем граням диска (среднее попадало между островами палитры). Теперь UV диска берётся с грани, ближайшей к оси колеса, UV покрышки с самой большой грани покрышки (`Tools/lod_cars.py`, `wheel_cyl`). LOD2 перегенерирован для всех 146 FBX, импорт повторён.
- Скриншоты `Docs/screens/lod_fix_lod0.png` (оригинал), `lod_fix_lod1.png`, `lod_fix_lod2_v2.png` (принудительно один уровень над кучей). Попиксельно LOD0 против LOD1 6,4% пикселей с разницей >40 (силуэты после децимации), средняя разница 4,6/255; LOD0 против LOD2 16,3% и 13/255 (принудительно вблизи, где LOD2 в игре не показывается). `Tools/imgdiff.py` (чистый Python, без PIL) положен в репозиторий.
- Пороги ПК подняты до 10/20 м по просьбе пользователя (после этой правки, вероятно, можно вернуть 6/12: белизна была багом, а не качеством LOD).

## Профиль после LOD: физика или рендер (12.09)
- Пользователь: на телефоне 60 FPS, если не смотреть на машинки, 14 FPS при ходьбе по куче со взглядом на неё. Предположение про глубину сферы активации (1,5 м вниз) проверено профилем: физика 0,1–0,5 мс, живых тел до 250, не она. Таблица и выводы в `OPTIMIZATION.md`, раздел «Профиль в редакторе после LOD». Узкое место рендер: 4,5–4,7 M треугольников (около 2 M на LOD0 в радиусе 3 м) и 800–1040 батчей при взгляде на кучу против 28 в стену.
- Для замера временно включался `PlayerSettings.enableFrameTimingStats` (возвращён в 0) и `Platform.IsMobile = true` в рантайме (сброшен).

## Атлас текстур и мобильные пороги без LOD0 (12.09)
- По профилю (см. выше) сделаны пункты 1 и 2. Мобильные пороги `LodDistancesMobile` = 0 / 4 м: на телефоне LOD0 (4483) не показывается вовсе, вблизи сразу LOD1 (1674), с 4 м LOD2 (775). ПК без изменений (10 / 20).
- `ArcadeCarImporter.BuildAtlas`: 14 текстур ARCADE (6 × 512² палитры и фонари, 8 × 1024² винилы) собираются в один атлас 2048×2048 по ячейкам 512 (винилы ужаты до 512 через Blit, на машинке 0,3 м и в руке разницы нет), копирование через RenderTexture + ReadPixels (исходники не readable), PNG в `Art/Textures/Arcade_Atlas.png` (sRGB, без альфы, мипы, Clamp, сжатие), материал `Art/Materials/Arcade/Arcade_Atlas.mat` на `SortThem/TexturedLit` с инстансингом. Старые 14 материалов `Arcade_*` удалены.
- `BuildMesh` теперь собирает один сабмеш: каждый сабмеш исходной детали извлекается отдельно (`ExtractSubmesh`), UV переписываются в ячейку атласа своего исходного материала (полпикселя отступа), и всё склеивается `CombineMeshes(mergeSubMeshes: true)`. Для LOD исходный материал сабмеша определяется прежним `MatchSubmesh`. Проверка: 450 мешей по одному сабмешу, у всех 150 префабов один материал, UV в [0,003; 0,999]. Проверка цветов: 6 машинок рядом с оригиналами ARCADE (URP Lit, свои текстуры) совпадают один в один (`Docs/screens/atlas_final_compare_crop.png`, сверху оригинал, снизу наш атлас), куча `atlas_pile_final.png`. Скриншоты `atlas_pile.png`, `atlas_close*.png`, `atlas_compare_*` это следы поиска бага ниже (старые GPU-буферы), не итог.
- Замер в редакторе, мобильный режим (`Platform.IsMobile = true`), до → после:

| Положение | Батчей | Треугольников | Вершин | PlayerLoop |
|---|---|---|---|---|
| на куче, взгляд вниз | 1037 → 319 | 4,7 M → 2,75 M | 7,4 M → 4,6 M | 3,85 → 3,26 мс |
| на куче, взгляд вперёд | 983 → 350 | 4,5 M → 3,3 M | 7,3 M → 5,4 M | 4,35 → 3,31 |
| над кучей | 815 → 298 | 3,2 M → 2,2 M | 5,3 M → 3,5 M | 3,78 → 2,97 |
| угол зала | 507 → 234 | 3,1 M → 3,1 M | 4,9 M → 4,9 M | 4,62 → 2,98 |

- Угол зала треугольников не потерял: там и так всё LOD2. Следующий резерв для него: LOD3 ~300 или отсечение по дистанции. Нужна пересборка WebGL и замер на телефоне.
- **Грабли при проверке атласа (12.09, ~1,5 ч):** после импорта машинки в редакторе рисовались не теми цветами (синий Endeis чёрный, красная Europa оранжевая), хотя данные мешей были верные: UV каждого треугольника совпадали с исходными ×0,25 (проверено кодом), атлас попиксельно совпадал с исходными текстурами, пробные квады с атласом рисовались верно. Причина: `SaveMesh` перезаписывал существующий Mesh-ассет через `EditorUtility.CopySerialized`, и редактор продолжал рисовать старые GPU-буферы (`UploadMeshData` не помогал, меш non-readable). Копии мешей `Instantiate(mesh)` в рантайме рисовались правильно и совпали с оригиналами ARCADE один в один (`Docs/screens/atlas_compare_crop_copy.png`, сверху оригинал URP Lit, снизу наш атлас). В билде такого нет, меши читаются с диска. Правка: `SaveMesh` переписывает существующий меш через API (`Clear` + `SetVertices/SetNormals/SetUVs/SetTriangles` + `UploadMeshData`), GUID и ссылки префабов сохраняются, GPU-данные обновляются. Пробовали удалять и создавать ассет заново и принудительный `ImportAsset(ForceUpdate)` мешей: на диске ссылки верные, но в памяти редактора префабы теряли меши (ссылались на убитые объекты), лечилось только `ForceUpdate` самих префабов. Атлас без мипмапов, как текстуры в паке ARCADE (палитры из мелких квадратов, мипы смешивают соседние цвета), сжатие оставлено.

## Профайлер веб-сборки на телефоне (12.09)
- Пользователь: после атласа и порогов 0/4 на телефоне всё ещё мало FPS (стоя ~40, при ходьбе 12–14). Сделан пункт меню `SortThem/Build WebGL (Profiler)` (Development + Autoconnect Profiler, папка `Builds/WebGL_Profile`), скрипт `Tools/serve_build.sh` (python http.server по Wi-Fi), профиль снят с телефона пользователя через окно Profiler редактора и `HierarchyFrameDataView`. Таблица и разбор в `OPTIMIZATION.md`, раздел «Профиль веб-сборки на телефоне».
- Итог: стоя упор в CPU рендера (16 мс: выдача вызовов, сортировка, отсечение, UI); при ходьбе спираль физики (шаг 11–19 мс × до 9 шагов за кадр из-за `Maximum Allowed Timestep` 0,33). Правки: мобильный шаг физики 1/30 и потолок 2 шага, солвер 4 итерации, радиус активации 1,0; ПК потолок 0,1 с; камера без сортировки непрозрачных по дистанции (`GameConfig.NoDistanceSort`). Нужна пересборка и повторный замер обоих сценариев.
- Побочно: в профайлерной сборке по http Addressables (локализация) не загружаются («Insecure connection not allowed»), поэтому `BuildTool` для профайлерной сборки временно разрешает http (`PlayerSettings.insecureHttpOption`) и возвращает настройку после сборки. В Chrome на Mac фоновая вкладка троттлится до 1 кадра/с (счётчик показывал 2–4 FPS при 6 мс работы), не путать с производительностью игры.
- Повторный замер на телефоне после правок: стоя 22,6 → 17,9 мс (56 FPS), при ходьбе 45 → 24,5 мс медиана и 178 → 41 мс худший кадр, физика ровно один шаг на кадр. Таблица в `OPTIMIZATION.md`. Сборка `Builds/WebGL_Profile` и `Tools/serve_build.sh` остаются для следующих замеров.
