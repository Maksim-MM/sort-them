# Порт Sort Them на консоли — лог

> Лог порта по этому проекту. Общий пайплайн — `PORTING_PIPELINE.md` в этой папке, канон SDK — `UPSCALE_SDK.md`. Журнал разработки самой игры — корневой `PROGRESS.md`, сюда только порт.

## Решения (23.09)

- **Платформы:** ПК (клавиатура+мышь, геймпад), консоли через Upscale SDK, WebGL (Яндекс Игры) остаётся целевой платформой. Точный список консолей: Switch 1 (девкит есть); PS5/PS4/Xbox — уточнить у пользователя.
- **Одна ветка, один код.** Платформенные различия — через asmdef с `includePlatforms`, `#if` только в фабрике реализаций, Build Profiles на платформу. Долгоживущих платформенных веток не заводим.
- **WebGL не выкидывается** (отклонение от пайплайна, где мобильный/веб-код удаляется). Тач-схема остаётся под `Platform.IsMobile`; плагин Яндекса (PluginYG2) пойдёт в отдельный asmdef `includePlatforms: ["WebGL"]`, на консольных сборках его кода не будет. Сетевых обращений, рекламы, покупок, аналитики в проекте нет — Stage 1 практически пустой.
- **Управление на ПК и консолях — через SDK** (`PlayerInputActionSet` поверх `InputActionsSet`, UI-слои SDK). Игра делалась под ПК и геймпад с нуля.
- **Пока не делаем:** трофеи/ачивменты, активности PS5, Light Bar (решение пользователя 23.09).
- **Глифы:** готовые наборы из `Assets/UpscaleSDK/Extras/Glyphs/`, SDK выбирает набор по платформе сам. Подтверждение/отмена в подсказках — только через семантические `GamepadAction.Apply/Cancel`, не позицией кнопки (см. `GOTCHAS.md`, Switch).
- **Лог порта** — этот файл, корневой `PROGRESS.md` не трогаем (там журнал игры).

## Что уже совпадает с требованиями SDK

- New Input System 1.19.0 (`activeInputHandler: 1`), Legacy Input не используется.
- Unity 6000.3.12f1 — в списке поддерживаемых SDK v2.1.
- Unity Localization 1.5.13 + Addressables + TMP — стадии 5.5/5.6 закрыты игрой.
- Сейвы за интерфейсом `ISaveStorage` (`Runtime/Save/`), одна реализация `PlayerPrefsSaveStorage`, подключение в `GameManager.cs:77`. Настройки (`Core/Settings.cs`, 7 ключей) пишут в `PlayerPrefs` напрямую — перевести на хранилище SDK.
- Геймпад частично сделан (22.09): привязки всех действий, `UI/ActiveDevice.cs` (активное устройство по реальному вводу), подписи кнопок в `ControlHints`, навигация в окнах.
- `Platform.cs` — только `IsMobile`.

## Открытые хвосты

- Шрифты: атласы статические, фолбэков нет — после любого изменения строк в таблицах пересобирать атласы (`SortThem/Loc/Fonts/Build CJK Atlases`, `Rebuild LiberationSans Atlas`), иначе новые символы будут квадратами. Глифы для клавиш в тексте (`LMB`, `RT` и т.п.) идут из ASCII — уже в базовом диапазоне.
- `LiberationSans SDF - Fallback.asset` больше не используется (снят из fallback-таблицы), в git он грязный после динамической догенерации — можно откатить.
- Вычитка переводов носителями — `Tools/loc/review_ui_all_langs.tsv`, список сомнительных мест выше.
- Удалить `Assets/_Game/Input/SortThemInput.inputactions`, поле `GameManager.InputAsset` (ссылка в сцене `Main`) и `Editor/InputSetup.cs` — ввод целиком на SDK. **Блокируется веб-этапом:** на WebGL нет платформенной сборки SDK, пока не появится `WebPlatform` (см. план, строка 5), старый ассет остаётся как единственный путь ввода для веба. Текущая WebGL-сборка с SDK в её нынешнем виде не стартует (падение в `RuntimeBootstrap`), собирать веб до веб-этапа не имеет смысла.
- Проверка на реальном геймпаде в Play пользователем: раскладка RT/LT (триггеры как кнопки через порог 0,5), Apply/Cancel в меню на DualSense, вибрация через SDK.
- `ui.hint` (старая строка подсказки HUD) в таблице не используется — можно удалить ключ.
- Консольные модули Unity и платформенные пакеты не установлены; валидатор SDK под WebGL/Standalone не работает — прогнать `Tools → UpscaleSDK → Validate` под консольной целью.
- Standalone-сборка для проверки Addressables + SDK в плеере не делалась (только Play в редакторе, включая bootstrap-сцену).
- Отключение контроллера на консоли: глифы и ввод не страдают (платформенный набор выбирается define'ами), но реакции «пауза + сообщение „подключите контроллер“» в игре нет. По коду SDK (проверено 24.09): `UPSInput.OnDeviceRemoved(DeviceType)` только бросается наверх из `NewUnityInputProvider`, подписан на него лишь `ActionButtonElement`; паузы/диалога SDK не делает. Нужна своя реакция в `GameManager`/`UiRoot` под консольными define'ами: на `Gamepad` removed — пауза + окно с просьбой подключить контроллер, на `OnDeviceAdded` — закрыть. Требование сертификации всех трёх платформодержателей, проверять на девките.
- Отклонение от пайплайна: UI на собственной фокус-навигации, компоненты `Layer/LayerNavigator/ActionButtonElement` SDK не используются (см. «Решения»).

## Особенности проекта

- UI строится кодом (`UiFactory`, `UiRoot`), не префабами: слои SDK (`Layer`, `ActionButtonElement`, `LayerNavigator`) прикручиваются в коде-генераторе; ловушки префабов из `GOTCHAS.md` не актуальны.
- Одна сцена `Main`; `Editor/BuildTool.cs` собирает по `Paths.MainScene`. Bootstrap-сцена SDK `UPSBootstrap` должна стать сценой 0 — добавить в Build Settings и в `BuildTool`. `SceneManager.LoadScene(int)` в коде игры: проверить grep'ом после добавления сцены.
- `Resources/` в `_Game` нет; локализация через Addressables.
- Под валидатор SDK: `companyName` = `DefaultCompany` → нужен ровно `Upscale Studio`; `bundleVersion` 0.1.0 подходит; заставка Unity включена (предупреждение валидатора, для консолей выключить).
- На машине пользователя установлены модули Android, WebGL, Windows, Mac. Консольные модули Unity — с порталов платформодержателей; до их появления вся интеграция проверяется на Desktop-платформе SDK.
- `Editor/BuildTool.cs` уже содержит `EnsureWebGLTarget()` (сброс кэша Addressables `[BuildTarget]`) — при появлении консольных сборок обобщить на любую целевую платформу.

## План

| # | Стадия | Что | Статус |
|---|---|---|---|
| 0 | Stage 0 | `/unity-analyze` → `ARCHITECTURE.md`, `CLAUDE.md` со станзой порта (лог порта → этот файл). Новый чат. | ⬜ |
| 1 | Импорт SDK | Newtonsoft (`com.unity.nuget.newtonsoft-json`), Core + Desktop + платформы; `Config` в `Resources/UpscaleSDK/`, `SavesSettings`, 6 наборов глифов; `UPSBootstrap` сценой 0; `companyName`; валидатор зелёный; в Play `[UPSRuntime]` без ошибок. | ✅ 23.09 (валидатор — только под консольной целью) |
| 2 | Stage 2–5 | `PlayerInputActionSet` (геймпад + клавиатура + мышь одним действием), гейтинг геймплея при открытом окне (`Deactivate()`), слои UI, глифы вместо текстовых подписей в `ControlHints`, `ActiveDevice` → `UPSInput.GetPrimaryDevice()`. | ✅ 23–24.09 (без Layer/LayerNavigator, `ActiveDevice` свой) |
| 3 | Stage 6 | `UpscaleSaveStorage : ISaveStorage` поверх `UPSSaves.Prefs` (base64 → string), готовность через `Prefs.IsReady`/`OnReady`; `Settings` через `Prefs`; триггеры сейва централизовать. Трофеи/активности — нет. | ✅ 23.09 |
| 4 | Stage 7 | Чеклист сертификации, Build Profiles на платформу. Консольные модули есть только на сервере сборки — консольные сборки и валидатор SDK прогоняются там; локально проверяются Desktop-сборка и редактор. Первая цель — Switch. | ⬜ |
| 5 | Веб | **Только после прохождения сертификации на консолях (в первую очередь Switch)** — решение пользователя 24.09. Структура (обсуждено 24.09): один WebGL-only asmdef `SortThem.Web` (`includePlatforms: ["WebGL"]`), в нём: (а) `WebPlatform : UpscaleSDK.Core.Platform` с регистрацией `PlatformProvider.SetPlatform<WebPlatform>()` под `#if UNITY_WEBGL` и `IFileSystem` поверх PlayerPrefs — без этого `RuntimeBootstrap` падает на `AddComponent(null)`, `UPSInput`/`UPSSaves` не поднимаются и `UPSBootstrap` не переходит в `Main`; (б) PluginYG2 целиком внутри этой папки/сборки (проверить его собственные asmdef и `.jslib`), настройки не в `Resources/`, компонентов плагина в сценах нет — веб-часть поднимается кодом через `RuntimeInitializeOnLoad`; (в) `YandexSaveStorage : ISaveStorage`; (г) реализация `IPlatformServices` (реклама, покупки, лидерборды) поверх Яндекса. В `SortThem.Runtime` — только интерфейс `IPlatformServices` с флагами `IsAvailable` и пустая реализация по умолчанию; экраны рекламы/магазина/лидеров показываются только при `IsAvailable`, на консолях их не существует. Проверка изоляции: в `BuildReport` консольной сборки не должно быть сборок `SortThem.Web`/`PluginYG*` — зашить в `BuildTool`. Старый `SortThemInput.inputactions` удалять только после появления `WebPlatform` (иначе на вебе ввод остаётся без реализации). Доку плагина дочитать на этой стадии: https://max-games.ru/plugin-yg/doc/ | ⬜ после сертификации |

Статус на 24.09: строки 1–3 сделаны, строка 4 (сертификация) и 5 (веб, после сертификации) не начаты. Локализация залита на все 19 языков, шрифты для всех 19 языков подключены (Liberation + Noto CJK по локали, статические атласы).

## Сделано

- 23.09 — папка `porting/` соседнего проекта скопирована в `Docs/porting/` (15 файлов). Прочитаны `PORTING_PIPELINE.md`, `README.md`, `UPSCALE_SDK.md`, `GOTCHAS.md`, `STAGE-0-ANALYSIS.md`, `STAGE-2-5`, `STAGE-5.6`, `STAGE-6`.
- 23.09 — **SDK установлен** пользователем в `Assets/UpscaleSDK/` (v2.1, байт в байт совпадает с копией соседнего проекта, канон `UPSCALE_SDK.md` применим без оговорок), Newtonsoft 3.2.2 в манифесте. Настроено: `Assets/_Game/Resources/UpscaleSDK/Config.asset` (6 наборов глифов Outline + White kb/mouse) и `SaveSettings.asset` (`XboxContainerName = SortThem`, автосейв 300 с); `UPSBootstrap` сценой 0, `Main` сценой 1 (`EditorBuildSettings`, `BuildTool` берёт список сцен оттуда); `companyName = Upscale Studio`; заставка Unity выключена; `SortThem.Runtime.asmdef` ссылается на `UpscaleSDK.Core`. Валидатор `Tools → UpscaleSDK → Validate` под WebGL-таргетом отвечает «Can't find any validator for this platform» — работает только под консольными целями.
- 23.09 — **Ввод через SDK.** `Runtime/Input/GameInput.cs`: `PlayerInputActionSet` (Move: LS+WASD+стрелки; Look: RS отдельно от `LookMouse` — разное масштабирование; Interact: ЛКМ + RT как триггер-кнопка с фронтом > 0,5; Place: ПКМ + LT; Jump: Space + South; Sprint: Shift + LS-press hold; Crouch: Ctrl + East; Next/Prev: RB/LB; Scroll: колесо; Ability1–3: 1/2/3 + D-pad ↑/←/→) и `UiInputActionSet` (Navigate: D-pad + LS + стрелки; Submit: Enter + `GamepadAction.Apply`, Press и Hold; Cancel: `GamepadAction.Cancel`; Pause: Esc + Start — в UI-наборе, чтобы работал при погашенном геймплее). Обёртка `GameAction` хранит источники как `IGlyphSource` и отдаёт глиф под конкретное устройство (`Glyph(HintDevice, index)`), не полагаясь на `GetPrimaryDevice()` SDK: на ПК с постоянно подключённым DualSense SDK всегда считал бы устройством геймпад. `GameManager.UiBlocking` → `GameInput.SetGameplayActive` → `PlayerInputActionSet.Deactivate()/Activate()`. `GameManager.Update` → `GameInput.Tick()` (фронты триггеров раз в кадр). `ActiveDevice` — латч по последнему реальному вводу на ПК, на консолях `#if` даёт `Gamepad` всегда. `Rumble` → `UPSInput.SetGamepadVibration/StopGamepadVibration`. Старый `SortThemInput.inputactions` и поле `GameManager.InputAsset` больше не читаются (ассет и поле оставлены, `InputAsset.Enable()` снят) — удалить отдельным шагом вместе с `Editor/InputSetup.cs`.
- 23.09 — **Глифы.** `UI/GlyphImage.cs` (Image + TMP-фоллбэк, обновление в `LateUpdate` при смене устройства/спрайта) и `UiFactory.Glyph(...)`. Подсказки способностей над слотами — глиф вместо текста в рамке; окно «Управление» — строка = набор `GlyphImage` (для «Выбор предмета» LB+RB на геймпаде, колесо на клавиатуре — лишние глифы скрываются сами); тутор — глиф слева от текста, ключи `tut.walk/look/take/place` переведены на форму без `{0}` («Походить», «Повертеть камерой»…), `UiRoot.Plain()` вычищает старые значения с плейсхолдером. `ControlHints` сведён к `Device/Glyph/Fallback`. Проверено в Play (Roslyn через MCP): 30 глифов резолвятся на обоих устройствах, набор PlayStation по подключённому DualSense; kb: `keyboard-wasd`, `T_Mouse_XY_Key_White`, `space`, `shift`, `ctrl`, `esc`, `1`; pad: `stick_left/right`, `trigger_r2/l2`, `button_cross/circle/options`, `shoulder_r1`, `d_pad_up`.
- 23.09 — **UI-ввод** (отклонение от пайплайна, см. «Решения»): `UiRoot.UpdateMenuFocus` читает `GameInput.Ui.Navigate/Submit/Cancel`, `UiRoot.Update` — `Ui.Pause`; `UiRoot.Upgrade` — `Submit.Held()` для удержания кнопки улучшения. Компоненты `Layer/LayerNavigator/ActionButtonElement` не внедрялись. Тест симуляцией клавиатуры в Play: W → Move (0, 1); 50 px мыши → 6,0° поворота; Esc → пауза открыта, `PlayerInputActionSet.IsActive = false`; ↓/↑ → фокус Save → Resume; Enter → пауза закрыта, набор снова активен; латч устройства → Keyboard.
- 23.09 — **Сейвы и настройки.** `Save/UpscaleSaveStorage.cs` (`ISaveStorage` поверх `UPSSaves.Prefs`, ключ `sortthem.save`, base64; при отсутствии в SDK — разовый импорт из `PlayerPrefs`; без готового SDK — `PlayerPrefsSaveStorage` как фоллбэк), `Save/SavesReady.cs` (`IsReady`/`Prefs` с гардами от NRE до инициализации), `Settings` пишет в `Prefs` (`GetBool` для вибрации), первое чтение — миграция из `PlayerPrefs`, `Flush()` → `TrySave()`. `GameManager.Start` ждёт `SavesReady.IsReady` до 10 с (фоллбэк с предупреждением), потом `Settings.Load()`, затем локализация и загрузка сейва. Ползунки настроек перечитывают значения при открытии окна (строятся до загрузки настроек). Проверено: `UpscaleSaves/SortThem.txt` создан, «save imported from PlayerPrefs into SDK storage», «save loaded». Сейв пользователя восстановлен из бэкапа `Tools/backups/PlayerPrefs_DefaultCompany_SortThem_2026-09-23.{plist,json}` прямо в файл SDK (см. GOTCHAS про `companyName`/`defaults write`).
- 23.09 — **Локализация, инфраструктура.** 17 новых локалей (`zh, zh-Hant, da, nl, fi, fr, de, it, ja, ko, no, pl, pt-BR, es, sv, tr, uk`) + `Locale_ru`, всем `FallbackLocale(en)`, `UseFallback = 1` у String и Asset Database, таблицы `Game_<code>` и Addressables-группы созданы `LocalizationEditorSettings`. Селекторы без изменений: CommandLine → System → Specific(en). Имена в переключателе: `CultureInfo.NativeName`, для `zh`/`zh-Hant` руками «简体中文»/«繁體中文» (у обоих NativeName «中文»). Исходник для переводчиков `Tools/loc/source_en_ru.{tsv,json}`, переводы — `Tools/loc/out/<code>.json`.
- 23.09 — **Переводы залиты на 17 языков** (машинный перевод пятью параллельными агентами по языковым группам, по 385 ключей, проверки: набор ключей, плейсхолдеры `{0}/{1}`, `$`, `× · …`; имена собственные `car.*` с одинаковыми en/ru скопированы без перевода; TRM-термин `ui.vibration` дословно по `LOCALIZATION_TERMS.md`; тон неформальный). Импорт — меню `SortThem/Loc/Import Translations (Tools/loc/out)` (`Editor/LocImport.cs`, отчёт `set/unknown` по локали, все 17 × 385 set, 0 unknown), обратный экспорт всех таблиц — `SortThem/Loc/Export All Tables` → `Tools/loc/export.tsv`. Сводка для вычитки носителями — `Tools/loc/review_ui_all_langs.tsv` (151 UI/сообщения/категории × 17 языков). **Сомнительные места, переданные переводчиками** (проверить носителю): названия кнопок PlayStation `pad.*` во всех языках (переводчики не уверены в официальной терминологии Sony; сейчас `pad.*` в UI не используются — глифы), `ui.on/ui.off` (fr «Oui/Non», it/es «Sì/No», pt-BR «Ligado/Desligado» длинно), `cat.cat_hatchbacks` (de «Kompaktwagen», no «Kombibiler», sv «Halvkombi»), ru/en расхождения в исходнике: `special.step.foglight` en «Roof lights» vs ru «Противотуманки», `cat.cat_service` en «Emergency & Service» vs ru «Спецслужбы» (как и `cat_emergency`) — переводчики шли по английскому. **Шрифты:** в проекте только `LiberationSans SDF` (+ динамический Fallback того же TTF) — латиница с диакритикой и кириллица есть, CJK нет: на `ja` в открытом меню 70 символов без глифов (квадраты). Нужны шрифт(ы) для ja/ko/zh/zh-Hant (по решению пользователя — позже; процедура атласов — `STAGE-5.6`).
- 23.09 — **Гонка локали на старте** (словлено в Play): при сохранённой локали `de` HUD переключился, а тексты окон, привязанные через `UiRoot.Bind`, остались английскими — `SelectedLocaleChanged` пришёл до загрузки таблицы `Game_de` (Addressables асинхронно), и `Loc.Get` вернул фоллбэк. Правка: `GameManager.WaitTable()` ждёт `StringDatabase.GetTableAsync(Loc.Table)` после установки локали на старте, а `OnLocaleChanged` перерисовывает UI через корутину после загрузки таблицы (это же покрывает переключение языка в настройках).
- 23.09 — Проверки в Play: bootstrap-цепочка (`UPSBootstrap` → сплэш → `UPSSaves.Prefs.IsReady` → `Main`) — сейв 871 загружен; окно «Управление» с глифами, скриншоты `Docs/screens/port_controls_glyphs.png` (en), `port_controls_de_keyboard.png` (de). Спрайты клавиш SDK 512×512 с большими прозрачными полями — глифы в UI ставятся размером 56–60 px, видимая клавиша ≈ 30 px.
- 24.09 — Окно «Управление» переделано по замечанию пользователя: рамки-капсы убраны, строка 50 px, глиф 64 px (у мыши и геймпада поля меньше — коэффициент 0,7 в `GlyphImage`), глифы прижаты к левому краю (0,1–0,4 ширины), подпись от 0,42; строка «Пауза» удалена (11 строк). Скриншоты `Docs/screens/port_controls_glyphs_v6.png` (клавиатура), `port_controls_gamepad.png` (виртуальный Xbox-пад). Замечено: ключ `ctl.pause` в таблице теперь не используется.
- 24.09 — **Шрифты для 19 языков.** Решение (обсуждено с пользователем): без TMP-фолбэков между разными гарнитурами — пользователь против смешения шрифтов в одном слове; вместо этого **смена основного шрифта по локали в одной точке**: латиница/кириллица (15 локалей) остаются на `LiberationSans SDF`, для `ja/ko/zh/zh-Hant` — `NotoSans JP/KR/SC/TC` (TTF взяты из `my-clothing-store-simulator/Assets/UI/Fonts`, лежат в `Assets/_Game/Fonts/`; в Noto своя латиница и цифры, поэтому строка целиком в одном шрифте). Рантайм: `UI/UiFonts.cs` (класс `Fonts`: `Register` из `UiFactory.Text`, `Apply(localeCode)` грузит `Resources/Fonts/<Noto> SDF` по требованию, переставляет шрифт всем живым текстам, перевешивает глоу через `UiFactory.ReapplyGlow`, потом `UnloadUnusedAssets`), вызывается из `GameManager.InitLocalization` и `RefreshLocalization`. Атласы — все статические, набор символов ровно из таблиц: `Editor/FontAtlasTool.cs` (`SortThem/Loc/Fonts/Build CJK Atlases (static)` и `Rebuild LiberationSans Atlas (static, Latin+Cyr)`), наборы символов выгружены в `Tools/loc/chars/<code>.txt` + `_latin_cyr_all.txt`, к каждому символу добавляются оба регистра (см. GOTCHAS: `UpperCase` применяется при рендере). Параметры: Liberation 86 pt / padding 9 (как было), Noto 64 pt / padding 7 (то же соотношение, глоу выглядит одинаково; при 86/9 SC/TC не влезали в 2048²). Итог: Liberation 343 символа 2048² (было 250 + динамический фолбэк с кириллицей, который догенерировался на лету), Noto JP 503 / KR 525 / SC 554 / TC 553 символа, по 2048² Alpha8 (4 МБ в памяти, загружен только текущий). Проверено: статически — все 19 таблиц в верхнем и нижнем регистре против своего шрифта, 0 пропусков; в Play — `ja/ko/zh/zh-Hant` окно «Управление» (все тексты на Noto, глоу заголовка на атласе Noto, 0 пропусков по `textInfo`), `ru` настройки с прописными Й/Ы/Ь/Ц. Скриншоты `Docs/screens/port_fonts_{ja,ko,zh,zhHant}_controls.png`, `port_fonts_ru_settings_v2.png` (`port_fonts_ru_settings.png` — с квадратами до правки регистра). Бэкап старого ассета — `Tools/backups/LiberationSans_SDF_2026-09-24_*.asset`.
- 24.09 — Подписи в настройках: на `ru` «Чувствительность по горизонтали» (403 px при 18 pt Bold UpperCase) не влезала в область подписи (0,52 ширины строки = 375 px) и уходила под ползунок. Замер `GetPreferredValues` по всем 19 локалям: самая длинная — ru, затем de 315 px, uk 306 px. Область подписи расширена до 0,58 (420 px), ползунок 0,60–0,87, значение 0,89–1,0; подписям строк включён TMP auto-size 18→13 как страховка от будущих правок текста (при текущих строках не срабатывает). Снимок `Docs/screens/port_settings_ru_v3.png`.
- 24.09 — HUD по просьбе пользователя: счётчики машинок/полок/ящиков 24 → 32 pt (как баланс), контейнер `Stats` 320×110 → 440×140, FPS сдвинут на −164; счётчик инвентаря 30 → 32 pt и получил ту же тень `TextGlow`, что у остальных. Список предметов инвентаря (18 pt) не трогали. Мобильная раскладка (`_inventoryCountText` 24 pt у колеса) без изменений. Начертания оставлены: баланс и счётчик инвентаря Bold, счётчики слева Regular. Снимок `Docs/screens/port_hud_sizes.png`.
- 24.09 — HUD: подписи «Машинки/Полки/Ящики» заменены иконками (просьба пользователя). Иконки — белый контур в стиле иконок способностей, но штрих толще (14 px на 256), генерируются кодом: `Editor/HudIconGen.cs` (SDF-растеризатор: отрезки, кольца, точки, аналитический антиалиасинг) → `Art/UI/stat_cars.png`, `stat_shelves.png`, `stat_crates.png`, меню `SortThem/3f. Generate HUD Icons` (пишет PNG и импортирует через `UiSpriteSetup.ImportStatIcons`). `UiSpriteSetup.StatIcons`, поле `UiRoot.StatIcons` (назначено в сцене `Main`, `LevelBuilder` назначает при пересборке). `UiRoot.StatRow`: строка 40 px, иконка 44 px с `Shadow`, число 32 pt с тем же `TextGlow`; `RefreshStats` пишет только числа. Ключи `ui.cars`/`ui.shelves` в HUD больше не используются (`ui.crates` остаётся в окне улучшений). Снимки `Docs/screens/hud_icons_preview.png`, `port_hud_icons_v2.png`. Дополнение: пользователь заметил разницу толщины между `$1` (Bold) и счётчиками слева (Regular) — счётчики переведены на Bold, штрих иконок поднят с 14 до 24 px (тонкие линии 11 → 18) под стем жирных цифр 32 pt (~4 px на экране), у колёс убраны втулки. Снимок `port_hud_icons_v3.png`.
- 24.09 — **Глиф Start для PS4.** В штатном `Extras/Glyphs/PlayStation4/Ps4 Outline IconsSet.asset` SDK v2.1 элемент `Glyph: 18` (`GamepadGlyph.Start`) указывает на спрайт PS5 `PlayStation5/Outline/button_options.png`. Из `my-clothing-store-simulator` скопирован спрайт `PlayStation5/Outline/Button - PS4 Options.png` (480×480, надпись OPTIONS над кнопкой DualShock 4; путь и guid `a75ce5c7…` сохранены как в старом проекте, чтобы наборы совпадали байт в байт) и набор PS4 Outline переведён на него. Colored-набор PS4 не трогали (игра использует Outline). В UI глиф Start сейчас не показывается (строка «Пауза» из окна управления удалена), правка на будущее.
- 24.09 — **Титульный экран «Press any button to start»** (просьба пользователя). Сцена `Assets/_Game/Scenes/Title.unity` между бутстрапом и игрой: Build Settings = `UPSBootstrap` → `Title` → `Main` (бутстрап SDK грузит индекс 1, его код не трогали). Компонент `UI/TitleScreen.cs`, UI строится кодом: фон `Art/Textures/Background+logo v2.png` через `RawImage` + `AspectRatioFitter` EnvelopeParent (картинка 1.79:1, края обрезаются, логотип не плывёт), надпись внизу 44 pt Bold с тенью, пульсирует. Порядок: ждёт `SavesReady`, `Settings.EnsureLoaded`, музыка — случайный трек из `GameConfig.MusicClips` на громкости настроек, затем локаль (сохранённая в настройках) + таблица + `Fonts.Apply`, и только потом `LoadSceneAsync(Main)` с `allowSceneActivation = false` (сцена с 3005 машинками готова к моменту нажатия; локализация грузится до предзагрузки, т.к. отложенная активация сцены может держать очередь асинхронных загрузок). `Loc.Ready` на титуле не трогается — иначе `GameManager` не получит событие `Loc.Changed` при старте `Main`. Ввод напрямую через Input System (у SDK нет понятия «любая кнопка»): любая кнопка геймпада кроме Start (Start зарезервирован под будущее меню настроек на титуле), стики не считаются (дрейф); на ПК любая клавиша кроме Esc и клики мыши. Текст по устройству: `title.press_button` / `title.press_key` (19 языков, атласы шрифтов пересобраны, 0 пропусков); на ПК с подключённым падом до первого ввода с клавиатуры/мыши показывается «кнопку». По нажатию: `Sfx.PlayUi(UiClickClip)`, `Rumble.UiClick()` только если нажали на геймпаде, затемнение и уход музыки за 0,45 с, `Rumble.Stop()`, активация `Main`. Сцену собирает `Editor/TitleSceneBuilder.cs` (меню `SortThem/Build Title Scene`: создаёт или обновляет сцену, камера с `AudioListener`, ссылки на фон/музыку/клик из `GameConfig`, `EnsureBuildScenes()`). Попутно: `LevelBuilder` при полной пересборке уровня перезаписывал Build Settings одной сценой `Main` (выкидывал бутстрап SDK) — теперь вызывает `EnsureBuildScenes()`. Проверено в Play с `UPSBootstrap`: титул, музыка `music_03`, предзагрузка 0,9; виртуальный геймпад — Start не запускает, надпись сменилась на «button», South → `Main`, `GameManager.Ready`, ошибок нет. Снимок `Docs/screens/port_title_en_key.png`. Play из `Main` напрямую работает как раньше (титул пропускается). Правка по замечанию пользователя: текст короче — «Press Any Button» / «Press Any Key» (все 19 языков заново, атласы пересобраны, 0 пропусков), пульсация масштабом 1,00–1,06 от центра вместо ухода в прозрачность. Снимки `port_title_en_v2.png`, `port_title_ja.png` (Noto JP, тень на месте).
- 23.09 — веб-сборка починена до порта (не часть порта, но влияет на общий код): IL2CPP `OptimizeSize` + стриппинг `Medium` для WebGL, `EnsureWebGLTarget()` в `BuildTool`; разбор в `OPTIMIZATION.md`.

## Производительность Switch: мобильный профиль LOD и физики (24.09)

Симптом на железе: до 15 FPS при взгляде на кучу, 60 при взгляде вверх; Quality-профиль Mobile выставлен. Причина: пороги LOD и настройки физики выбирались по `Platform.IsMobile` (`Application.isMobilePlatform`), на Switch он ложный, поэтому Switch получал ПК-пороги 10/20 м (вблизи LOD0 по 4483 треугольника; по замерам в редакторе 12.09 на куче 11,6–15 M треугольников против 2,75 M с мобильными порогами) и ПК-физику (шаг 0,02, 6 итераций, радиус 3 м). Правка: `Platform.LowPower` (на `UNITY_SWITCH` всегда true, иначе `IsMobile`) в `GameManager.ApplyPlatformSettings`, радиусе активации и `CarLod.Tick`. `IsMobile` на Switch не меняли (тач-ввод и мобильная раскладка UI). Замер на железе за пользователем.
